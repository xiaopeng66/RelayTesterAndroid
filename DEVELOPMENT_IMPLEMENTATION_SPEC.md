# Relay Tester Android 重构开发与验收文档

**版本：** 1.2  
**日期：** 2026-08-30  
**目标版本：** `0.7.0`  
**技术栈：** Kotlin、Jetpack Compose、Material 3、DataStore、Android Keystore、OkHttp

---

## 1. 本次重构的目标

本次工作将 Relay Tester 从“单个站点配置页 + 单站点余额页”升级为更适合日常运维的 Android 原生工具：

1. 消除页面纵向滚动的卡顿感，尤其是模型测试结果多、模板编辑字段多、批量测试运行中的情况。
2. 将模型测试页的供应商选择器改为双栏卡片，不再依赖横向滑动。
3. 将余额页改为“全部供应商余额总览”：双栏卡片、可用余额、总额度、已用比例圆环、批量查询、单卡片选中后配置凭据。
4. 让 new-api 用户 ID 成为可选项：填写时发送 `New-Api-User`，留空时不发送该请求头。
5. 清理余额页的冗余/误导性文案，包括 `cc-switch` 暴露文案、重复的 `GET /api/user/self` 和密集难读的安全说明。
6. 统一标题、图标、色彩、控件尺寸和信息层级，使视觉风格达到成熟商业化工具应用的标准。
7. 在不泄露 API Key、PAT 或站点响应详情的前提下，完成代码风险检查与模拟器验证。

本次不改变以下已有原则：

- 余额模板仍然是受限的声明式 HTTP 配置，不执行 JavaScript、Shell 或任意 Kotlin 代码。
- API Key 和余额 PAT 仍由 Android Keystore 加密保护；它们不写入模板、测试结果、导出文件或错误提示。
- 自定义余额模板仍然只允许访问当前供应商的 HTTPS 主机。

---

## 2. 已完成的基线调查

### 2.1 真实模拟器环境

| 项目 | 实际环境 |
|---|---|
| 设备 | `RelayTesterApi35` AVD，Pixel 6 配置 |
| 系统 | Android API 35 |
| 分辨率 | 1080 × 2400，420 dpi |
| 包名 | `com.relaytester.app.debug` |
| 测量方式 | `dumpsys gfxinfo`、真实 ADB 滑动、页面截图、`logcat` |

### 2.2 初始性能数据

以下数字来自冷启动后，在模拟器上对已安装 Debug 包进行的往返滑动测试；不是只依据编译结果推测。

| 页面与场景 | 帧数 | 卡顿帧 | P90 | P95 | P99 | 结论 |
|---|---:|---:|---:|---:|---:|---|
| 模型测试页，常规滑动 | 844 | 1.78% | 18 ms | 18 ms | 32 ms | 常规滑动基本可用，但仍有绘制慢帧 |
| 余额总览，常规滑动 | 1,112 | 0.09% | 18 ms | 18 ms | 18 ms | 当前样本流畅 |
| 模板编辑页，常规滑动 | 1,067 | 2.25% | 18 ms | 18 ms | 22 ms | 多个大表单卡片的绘制压力较高 |
| 模板编辑页，快速滑动 | 548 | 3.10% | 22 ms | 25 ms | 32 ms | 有可感知的快速滑动抖动风险 |

`logcat` 未出现 `AndroidRuntime` 崩溃，也没有发现垃圾回收停顿日志。慢帧主要归类为 `Slow issue draw commands`，而非网络、GC 或主线程 I/O。

### 2.3 已确认的性能风险

1. 模型测试配置卡和模板编辑卡使用 `animateContentSize` 包裹整张大卡片。展开/收起或卡片内状态变化时，会触发更大范围的重新测量和绘制。
2. 模型测试页每次状态重组都可能重新过滤模型列表；有测试结果时还会重新过滤、排序结果。
3. 批量测试每完成一个模型便更新整个 `results` 列表。模型数量较大或并发较高时，这会形成高频列表复制、排序、卡片重组。
4. 供应商横向列表在视觉上没有利用屏幕宽度，也增加了横向/纵向手势认知成本。
5. 余额页目前只保留一个 `latestSnapshot`，不能满足多供应商总览和批量刷新。

---

## 3. 页面信息架构

```text
Relay Tester
├── 模型测试
│   ├── 供应商双栏选择区
│   ├── 站点配置
│   ├── 测试参数 / Prompt 预设 / 高级设置
│   ├── 已获取模型 / 测试结果（同一纵向列表）
│   ├── 测试后才出现的汇总、筛选、导出
│   └── 安全提示
└── 余额
    ├── 全部站点余额（双栏卡片 + 批量刷新）
    ├── 当前选中站点的余额查询凭据
    ├── 当前选中站点的查询详情与单站点刷新
    ├── 当前选中站点的模板选择和模板管理
    ├── 紧凑型模板安全说明
    └── 余额模板编辑器
```

模型测试和余额共用当前供应商。余额页点击某一站点卡片会选择该供应商，并在卡片网格下方显示该站点的凭据、模板和详细结果；回到模型测试页时仍然是同一个站点。

---

## 4. 交互与功能规格

### 4.1 顶部品牌区与一级导航

- 顶部仅保留一个统一的品牌组件，避免模型测试页和余额页各自维护相近但不一致的实现。
- 图标放置在 32dp 的浅色容器内，图标和标题之间固定 12dp 空隙，避免当前图标与 `Relay Tester` 标题过近。
- 标题为 `Relay Tester`，副标题由页面提供：
  - 模型测试：`中转站模型批量验证`
  - 余额：`全部站点余额总览`
- 下方使用两项一级 Tab：`模型测试`、`余额`。Tab 保持固定高度，点击不会造成布局位移。
- 一级 Tab 切换不保留重量级页面过渡动画；以立即切换保证触摸响应优先。

### 4.2 模型测试页的供应商双栏卡片

- 用两列 `FlowRow` 取代横向 `LazyRow`，不得出现“向左/向右滑动查看供应商”的主要路径。
- 每个卡片最小高度 96dp，显示：供应商名、协议、模型数量。
- 选中卡片使用 `secondaryContainer` 色调和清晰边框；未选中卡片为普通 Surface。
- `添加供应商`作为同尺寸卡片占用最后一个网格位置。
- 每个可点击区域不小于 48dp，卡片具备可访问的选中状态。

### 4.3 已获取模型与测试结果使用同一列表

点击“获取模型”成功后，模型不得再以可左右滑动的横向 Chip 行展示。实现为主页面 `LazyColumn` 中的纵向模型卡片列表，并与测试后的结果列表共享同一视觉结构和稳定 key。

| 阶段 | 列表显示内容 | 不显示的内容 |
|---|---|---|
| 获取模型后、尚未开始测试 | 模型名、`已获取，尚未开始测试` | 延迟、Token、错误、汇总、筛选、导出 |
| 测试运行中 | 同一模型卡片原位变为等待/成功/失败状态，显示进度 | 无关的导出结果提示 |
| 测试完成后 | 模型名、状态、延迟、Token、错误；显示汇总、筛选、复制、导出 | 无 |

实施要求：

1. 只保留一组纵向卡片，不复制模型列表和测试结果列表。
2. 获取模型后的卡片 key 为模型名；开始测试后沿用相同 key，避免列表闪烁或错误复用。
3. `ModelListCard` 中的横向 `LazyRow` 必须删除。
4. 测试前不显示结果筛选、结果汇总、重测、复制或导出操作；这些属于测试后的参数与操作。
5. 关键词过滤在获取模型后和开始测试时保持同一规则，页面明确显示“匹配 x / 总计 y”。

### 4.4 余额总览与双栏卡片

余额页在首屏提供一个满宽标题行：`全部站点余额`、更新时间提示和“批量查询”按钮。

每个供应商使用固定双栏余额卡片，卡片内容如下：

| 区域 | 内容 |
|---|---|
| 顶部 | 供应商名称；选中状态或查询状态 |
| 左下 | `可用` 金额（按模板除数换算后的值） |
| 右下 | `总额` 金额；接口未提供时显示 `—` |
| 右侧圆环 | 已用量比例：`used / total`；无总额时显示灰色空环 |
| 底部 | 套餐名或状态：未查询、查询中、需要配置、请求失败、更新时间 |

圆环实现要求：

- 使用 Compose `Canvas.drawArc` 绘制，不引入图表 SDK。
- 背景环为 `surfaceVariant`，进度环为品牌色；失败状态使用错误色。
- 百分比只在总额为正数时显示，范围限制为 0%–100%。
- 颜色不是唯一信息来源；文本仍明确给出“已用 xx%”或状态。

点击卡片的流程：

1. 保存当前站点尚未保存的设置和凭据。
2. 切换当前供应商。
3. 在网格下方显示新站点的“余额查询凭据”、“查询详情”和“余额模板”。
4. 不自动发起网络请求，不滚动劫持用户位置。

### 4.5 单站点与批量余额查询

- `查询当前站点`：仅查询当前选中的供应商。
- `批量查询`：顺序查询所有供应商，避免向大量中转站瞬时并发发请求，也减少限流风险。
- 批量过程中按钮显示 `正在查询 x / y`，当前被查询的卡片显示加载状态；其他卡片保持上次结果。
- 某个站点失败不终止后续站点。失败原因以脱敏后的短文本写入对应卡片，例如：`未配置 PAT`、`未配置 Base URL`、`网络连接失败`。
- 批量结束显示 `已更新 x / y 个站点`；如果有失败，则显示 `x 成功，y 需要处理`。
- 查询结果仅留在内存中；再次启动 App 不把过时余额伪装成实时数据。

### 4.6 凭据与 new-api 模板

当前选中的供应商显示独立的凭据表单：

| 字段 | 是否必填 | 说明 |
|---|---|---|
| 余额查询访问令牌（PAT） | 当模板引用 `{{accessToken}}` 时必填 | 加密保存到 Android Keystore |
| 用户 ID | **选填** | 仅在需要指定 new-api 账户时填写 |

new-api 内置模板固定为：

```text
GET /api/user/self

Authorization: Bearer {{accessToken}}
User-Agent: RelayTester/1.0
New-Api-User: {{userId}}   （留空时不发送）

available = data.quota
used      = data.used_quota
total     = available + used
plan      = data.group
scale     = 500000
unit      = USD
```

说明：

- 界面展示名和说明统一使用 `new-api`，不再显示 `cc-switch`。
- 用户 ID 空白时，`New-Api-User` 请求头被完全省略，而不是发送空字符串。
- 模板详情中只显示一次 `GET /api/user/self`；不再同时在说明和请求地址处重复展示。

### 4.7 模板区文案与按钮

- 内置模板卡只显示：`new-api 余额查询`、一句简短说明和一个请求摘要。
- 内置模板操作使用满宽短按钮 `复制为自定义模板`，不再使用容易因两栏宽度换行的“复制并编辑”。
- `新建模板`作为独立满宽次级按钮，避免与复制按钮争夺狭窄宽度。
- “模板安全与占位符”改为紧凑说明：一行可用占位符标签 + 两句安全说明，禁止堆叠大段长文本。
- 编辑器中的占位符说明也使用标签/短句，保留所有功能字段但降低认知密度。

---

## 5. 视觉规范

### 5.1 设计方向

定位为“可靠、清晰、偏专业运维”的移动工具，而不是极端技术感或大面积纯黑页面。重点是数据扫读、状态区分、稳定操作感。

| 设计项 | 规范 |
|---|---|
| 品牌主色 | 深蓝 `#155EEF`，用于主操作、活动 Tab、进度环 |
| 成功色 | 青绿 `#078A78`，用于成功状态和套餐标记 |
| 警示/错误 | 使用 Material 错误色，不使用高饱和红色大面积铺底 |
| 背景 | 浅色 `#F8FAFC`，卡片为白色或语义容器色 |
| 深色模式 | 深蓝灰背景，确保正文与背景对比度至少 4.5:1 |
| 形状 | 卡片 16dp、输入框 12dp、品牌图标容器 12dp |
| 间距 | 使用 4dp 基线；卡片内 16dp、卡片间 12dp、页面边距 16dp |
| 触控 | 所有按钮、IconButton、Tab 至少 48dp |
| 动效 | 仅保留点击涟漪、加载指示和必要的局部显隐；不得在滚动期间触发布局动画 |

### 5.2 字体层级

| 层级 | 目标规格 | 使用位置 |
|---|---|---|
| 产品标题 | 20sp / 26sp SemiBold | 顶部 `Relay Tester` |
| 页面/区块标题 | 16sp / 22sp SemiBold | `供应商`、`全部站点余额` |
| 卡片主数据 | 20sp / 26sp SemiBold | 可用余额、总额 |
| 正文 | 14sp / 20sp Regular | 描述、错误原因 |
| 标签 | 12sp / 16sp Medium | 状态、协议、时间 |

控制原则：同一视口中不得让过多文本同时使用大号标题。URL、模板路径、原始数值使用较小的等宽标签层级。

---

## 6. 性能实施设计

### 6.1 已执行/待验证的基础优化

1. 移除包裹整张大卡片的 `animateContentSize`。高级设置和高级响应映射保持直接展开，避免滚动测量波动。
2. 为 `LazyColumn` 的固定项和结果项指定 `key` 与 `contentType`。
3. 模型筛选结果通过 `remember(draft.models, draft.keyword)` 缓存；结果筛选/排序只在结果、筛选条件或搜索词变化时计算。
4. 为跨层传递的 Compose UI 数据模型标记 `@Immutable`，降低 Compose 对稳定性的保守重组判断。

### 6.2 批量模型测试更新节流

批量测试不能再“每完成一个模型就立即复制完整结果列表并重排 UI”。实施方式：

1. 启动测试时建立稳定的 `MutableList<ModelTestResult>` 和 `model -> index` 索引。
2. 网络完成回调只更新对应索引。
3. 最多每 120ms 向 Compose 发布一次不可变列表快照；最后一个结果强制立即发布。
4. 结果筛选、汇总和列表渲染只响应节流后的状态。
5. 取消任务或完成任务时立即发布最终状态，不丢失任何结果。

这项实现的目的是把模型测试进行时的 UI 刷新限制在约每秒 8 次以内，同时保持进度可感知。

### 6.3 列表策略

- 模型测试主页面继续使用单一 `LazyColumn`，不得嵌套纵向可滚动列表。
- 获取模型后的待测试项和测试结果使用同一 `LazyColumn` 的结果卡片结构；不得以横向 `LazyRow` 展示模型清单。
- 供应商网格使用非滚动 `FlowRow`，因为它只承担已配置站点的快速选择；超大量站点场景将在后续版本切换为单独供应商管理页。
- 余额页使用单一 `LazyVerticalGrid`；标题、操作区、凭据、模板区使用跨两列的全宽项，供应商余额卡使用单列项。
- 结果项和余额卡不得在组合函数内做网络、文件或 Keystore 读取。

### 6.4 验收阈值

在 API 35 模拟器快速来回滑动 10 轮后：

| 场景 | P95 目标 | P99 目标 | 卡顿帧目标 |
|---|---:|---:|---:|
| 模型测试页，配置区 | ≤ 22ms | ≤ 32ms | ≤ 3% |
| 余额总览，双栏卡片 | ≤ 22ms | ≤ 32ms | ≤ 3% |
| 模板编辑页，高级映射展开 | ≤ 24ms | ≤ 36ms | ≤ 4% |
| 测试运行中结果列表 | UI 不出现连续掉帧或卡死；以节流后帧数据复核 |  |  |

模拟器存在 GPU 虚拟化波动，因此以 P95/P99、慢帧类型和重复测试趋势共同判断；不以单一“卡顿帧为零”作为不现实标准。

---

## 7. 数据结构与接口设计

### 7.1 余额状态

`BalanceUiState` 从单个 `latestSnapshot` 改为：

```kotlin
data class BalanceUiState(
    val suppliers: List<SupplierProfile>,
    val activeSupplierId: String?,
    val templates: List<BalanceQueryTemplate>,
    val balanceSnapshots: Map<String, BalanceSnapshot>,
    val balanceErrors: Map<String, String>,
    val queryingSupplierIds: Set<String>,
    val isQuerying: Boolean,
    val batchProgressDone: Int,
    val batchProgressTotal: Int,
    val credentials: BalanceCredentialsDraft,
    // ...模板编辑状态与消息状态
)
```

- `balanceSnapshots`：供应商 ID → 当前运行期余额快照。
- `balanceErrors`：供应商 ID → 可安全展示的最后一次查询失败原因。
- `queryingSupplierIds`：查询中的供应商，用于卡片加载状态。
- 快照和错误不会持久化，防止旧余额被误认为实时数据。

### 7.2 查询接口

ViewModel 暴露：

```kotlin
fun queryBalance()       // 当前供应商
fun queryAllBalances()   // 顺序刷新全部供应商
fun selectSupplier(id: String)
```

内部将新增按供应商准备请求的函数：

```kotlin
private suspend fun prepareBalanceRequestFor(
    profile: SupplierProfile,
    preferredTemplate: BalanceQueryTemplate? = null,
): BalanceRequestPreparation
```

职责：

1. 校验 Base URL 和模板。
2. 从 KeyStore 安全读取该供应商对应的模型 API Key / PAT。
3. 检查模板需要的凭据；`{{userId}}` 不再要求非空。
4. 返回可执行请求或可展示的脱敏失败原因。

### 7.3 模板请求构建

`BalanceApi.buildRequest` 解析每个请求头后执行：

```kotlin
if (resolvedHeaderValue.isNotBlank()) {
    builder.header(name, resolvedHeaderValue)
}
```

因此 `New-Api-User: {{userId}}` 在用户 ID 空白时不会产生空请求头；其他自定义模板使用空占位符时也不会发送空请求头。

### 7.4 密钥边界

| 数据 | 保存位置 | 能否进入 DataStore | 能否导出 |
|---|---|---:|---:|
| 模型 API Key | Android Keystore 加密的 app-private 偏好 | 否 | 否 |
| 余额 PAT | Android Keystore 加密的 app-private 偏好 | 否 | 否 |
| 用户 ID | 供应商配置 | 是 | 否 |
| 模板 | DataStore | 是 | 否 |
| 余额快照 | 仅 ViewModel 内存 | 否 | 否 |

---

## 8. 代码问题检查清单

以下问题必须在交付前逐项复核：

- [x] 不再存在 Android ICU 不兼容的占位符正则；现有正则已在 API 35 启动路径初始化。
- [x] 内置 new-api 模板不要求用户 ID；空 ID 会在 `BalanceApi.buildRequest()` 中省略 `New-Api-User`。
- [x] 选择、添加、删除供应商后，余额快照和错误 Map 按供应商 ID 隔离；删除时同步剔除对应记录。
- [x] 保存模板后只使当前供应商的快照失效，不清除其他供应商快照。
- [x] 批量余额查询按站点顺序执行，单个站点的准备或请求失败只写入该站点错误并继续后续站点。
- [x] 单站点查询保存当前未保存的表单值后再请求；批量查询不会覆写其他站点凭据。
- [x] 模型批量测试使用稳定索引与 120ms 发布节流，完成时强制发布最终结果；取消会终止任务并保留已发布状态。
- [x] 结果列表的 key 使用模型名，余额网格的 key 使用供应商 ID。
- [x] 不向日志、Snackbar、导出 JSON、界面或文档写入 API Key/PAT 原文。
- [x] 输入控件、圆环和双栏卡片已在 API 35 的浅色、深色、1.3 倍字体和横屏截图中复核。
- [x] 不存在横向主流程的供应商滑动列表、重复 endpoint 文案、`cc-switch` 用户可见文本或超长安全说明。

---

## 9. 实施顺序

### 阶段 A：基础与性能（先完成）

1. 统一顶部品牌组件，移除 Tab 切换的重量级页面动画。
2. 完成整卡片动画移除、列表 content type/key、派生数据缓存。
3. 实现批量模型测试状态节流。
4. 将供应商选择器改为双栏卡片。

### 阶段 B：余额数据与交互

1. 扩展 `BalanceUiState` 为多供应商快照/错误/进度状态。
2. 实现当前站点查询、顺序批量查询和卡片选中。
3. 实现固定双栏余额网格与已用圆环。
4. 调整凭据区、new-api 可选用户 ID、模板文案与请求头规则。

### 阶段 C：视觉统一与可访问性

1. 更新语义色板、字号、间距、状态色。
2. 逐页截图检查：标题图标间距、双栏压缩、文案截断、按钮换行、深色模式。
3. 检查每个交互控件的最小触控面积和 content description。

### 阶段 D：验证与交付

1. 运行 `lintDebug` 与 `assembleDebug`。
2. 安装到 API 35 模拟器，不清除应用数据。
3. 冷启动、站点切换、凭据显示/隐藏、new-api 可选 ID、批量查询失败继续、模板复制编辑、系统返回全部手工复测。
4. 采集各页面的 `gfxinfo` 帧数据、检查 `AndroidRuntime`。
5. 读取 APK 元数据并计算 SHA-256，更新 README 和本文件的验证记录。

---

## 10. 交付验收标准

只有同时满足以下条件才交付 APK：

1. App 能在 API 35 模拟器冷启动，未出现 `AndroidRuntime`、ANR 或返回键异常退出。
2. 供应商选择器是双栏卡片，不需要横向滑动。
3. 余额页展示全部供应商的双栏卡片，包含可用、总额和已用圆环；可批量查询。
4. 点击余额卡片后可编辑对应站点的凭据和模板；其他站点凭据不会混用。
5. new-api 的 PAT 正常必填，用户 ID 为选填；ID 为空时请求头不发送。
6. 用户可见文案不包含 `cc-switch`，内置模板不重复显示同一 endpoint，安全说明可快速扫读。
7. “复制为自定义模板”在手机宽度下不换行、不截断。
8. 满足第 6.4 节性能阈值或给出可复现的模拟器局限说明；不存在连续滑动卡死。
9. `lintDebug`、`assembleDebug`、安装、实际滑动、日志检查、APK 校验均完成并记录。

---

## 11. 实施记录

| 阶段 | 状态 | 说明 |
|---|---|---|
| 需求与基线调查 | 已完成 | 已采集 API 35 模拟器截图、真实滑动和 gfxinfo 基线 |
| 本开发文档 | 已完成 | 本文件为后续实现的唯一实施依据 |
| 阶段 A | 已完成 | 统一顶部栏；供应商改双栏；移除整卡片动画；为列表添加 key/contentType、缓存和 120ms 批量结果节流 |
| 阶段 B | 已完成 | 实现多供应商余额快照/错误/进度、顺序批量查询、双栏余额网格、圆环、可选用户 ID 与请求头省略规则 |
| 阶段 C | 已完成 | API 35 浅色/深色、横屏、1.3 倍字体、控件语义与 48dp 触控区域复核完成 |
| 阶段 D | 已完成（有外部接口限制） | `lintDebug`、打包、覆盖安装、冷启动、连续滑动、模板返回、日志、APK 元数据与 SHA-256 均已完成；未调用用户真实余额/模型端点 |

---

## 12. 历史实施与验证记录：v0.4.0（2026-08-30）

### 12.1 已实施的关键改动

1. **获取模型后的同列表交互**：移除模型横向 Chip 展示。获取成功后，模型以 `ResultItem` 纵向卡片显示“已获取，尚未开始测试”；开始测试后以同一模型名 key 原位转为等待、成功或失败卡片。测试前不显示结果汇总、筛选、重测、复制、导出。
2. **滚动与批量测试性能**：移除大卡片 `animateContentSize`，避免嵌套纵向滚动；固定项和结果项具有稳定 key/content type；模型筛选和排序通过 `remember` 缓存；批量测试更新节流为最多每 120ms 一次界面快照。
3. **站点与余额总览**：模型测试页供应商和“添加供应商”均为双栏卡片。余额页以跨两列概览 + 双栏供应商卡片 + 当前站点的凭据/详情/模板区域组织；卡片显示可用、总额和已用比例圆环，批量查询逐站继续处理失败。
4. **new-api 与模板**：内置模板的用户可见名称为 `new-api`，User-Agent 为 `RelayTester/1.0`。PAT 必填、用户 ID 选填；用户 ID 空白时不发送 `New-Api-User`。模板仍是受限 HTTP 数据配置，不执行用户脚本。内置模板使用“复制为自定义模板”创建副本。
5. **文案、可访问性与安全**：清除了用户可见的 `cc-switch`、重复 endpoint 和大段占位符说明；供应商卡片带选择语义和可访问名称；密码字段保持默认遮罩；未新增将密钥写入 DataStore、日志、导出或截图说明的路径。

### 12.2 构建与静态检查

| 项目 | 结果 |
|---|---|
| Kotlin 编译 | `:app:compileDebugKotlin` 通过 |
| Lint | `:app:lintDebug` 通过，**0 error**；仅保留 4 条非阻断的上游版本/自适应图标目录警告 |
| Debug 打包 | `:app:assembleDebug` 通过 |
| 包名 | `com.relaytester.app.debug` |
| APK 版本 | `0.4.0-debug`，versionCode `6` |
| APK 大小 | `17,865,239` bytes |
| SHA-256 | `6FBB63F88A30B339C9020BDFCDF5988048BEAAD693A193C3D81DB159A5EC5514` |

本机 Gradle 的 Java 17 在 Windows Unix 域套接字初始化时出现 loopback 异常。验证期间通过临时 JVM 属性 `-Djdk.net.unixdomain.tmpdir=C:\__relaytester_force_tcp__` 使 JDK 回退到 TCP 管道，未修改应用代码、未修改用户数据、未降低应用网络安全策略。

### 12.3 API 35 模拟器验证

验证设备为 `RelayTesterApi35`（Android API 35、1080 × 2400、420 dpi）。所有安装均使用 `adb install -r` 覆盖升级，**未执行卸载或清除应用数据**。

| 场景 | 实测结果 |
|---|---|
| 覆盖安装与冷启动 | versionCode 6 安装成功；多次冷启动 `1.54–1.72 s`；`MainActivity` 位于前台 |
| 崩溃检查 | 冷启动、Tab 切换、模板编辑器进入/系统返回、深色/浅色和旋转后检查 `AndroidRuntime`，未发现崩溃 |
| 模型测试页 | 顶部图标与标题间距、供应商双栏、同规格“添加供应商”卡片、Prompt 预设和配置控件截图复核通过 |
| 余额页 | 双栏余额卡、可用/总额/圆环、PAT 遮罩、可选用户 ID、批量查询按钮与“复制为自定义模板”文本截图复核通过 |
| 深色 / 横屏 / 大字体 | 深色模式、横屏、1.3 倍系统字体截图通过；无控件遮挡或横向溢出 |
| 可访问性 | UI Automator 验证余额供应商卡导出为可点击 `Button`，具有“选择供应商 …”名称；批量查询、文本输入和密码显示按钮均保留可点击节点 |
| 模板返回 | 打开内置模板的复制编辑页后按系统返回，仍回到余额页而不退出 Activity |

### 12.4 连续滚动帧数据

测试方式：应用位于前台，在配置区/余额总览以 750ms 手势上下往返各 10 轮；每个场景前后以 `gfxinfo reset` 分段采集。模拟器的系统通知提示“Serial console enabled, performance is impacted”，因此同时记录 P95/P99 和慢帧类型，不以单个慢帧认定功能卡顿。

| 场景 | 帧数 | 卡顿帧 | P90 | P95 | P99 | 慢帧类型 | 结论 |
|---|---:|---:|---:|---:|---:|---|---|
| 模型测试配置区 | 1,322 | 3.33% | 10ms | 17ms | 18ms | 44 个 `Slow issue draw commands`；无慢 UI 线程/位图上传 | P95/P99 达标，无连续掉帧；卡顿帧略高于 3% 的伸展目标，受模拟器图形环境影响，需以后续真机复测为准 |
| 余额总览 | 1,305 | 2.68% | 10ms | 17ms | 18ms | 32 个 `Slow issue draw commands`；无慢 UI 线程/位图上传 | 达到本文件阈值 |

### 12.5 外部接口验证边界

模拟器中现有供应商指向示例地址，未配置可安全调用的真实模型列表/余额服务。为避免向错误端点发送用户的 PAT 或 API Key，本次没有伪造请求、篡改已保存站点、清除数据或调用真实凭据。因此，“获取模型后待测卡片 → 开始测试后原位更新”的网络回调已通过 Kotlin 编译、稳定 key/状态流代码审查验证，但仍需要在用户提供的可测试站点上做一次真实接口回归；这不影响 APK 的安装、启动、布局、滚动和本地状态安全验证。

---

## 13. 启动链路与紧凑布局第二轮优化：v0.5.1（2026-08-30）

本节以 [STARTUP_LAYOUT_PERFORMANCE_OPTIMIZATION_SPEC.md](STARTUP_LAYOUT_PERFORMANCE_OPTIMIZATION_SPEC.md) 为本轮详细验收依据。本节只记录最终实施和复测结果；第 12 节保留为 v0.4.0 的历史记录，不再代表当前 APK。

### 13.1 本轮启动性能改动

1. **恢复工作离开首屏主线程**：DataStore 读取、完整 JSON 反序列化、内置 `new-api` 模板整理、活动站点 API Key/PAT 的 Keystore 解密均在 `Dispatchers.IO` 内完成；恢复完成后分别一次性发布模型页和余额页的完整不可变状态。不再在无数据变更的启动中回写完整 DataStore。
2. **Factory 和网络客户端按需创建**：`MainActivity` 使用 `remember(applicationContext)` 保存 ViewModel Factory。Factory 只注入存储与密钥仓库；`RelayApi`、`BalanceApi`、`BatchTestRunner` 不再随启动构造，在用户获取模型、开始测试或查询余额时才于后台创建。模型列表请求和余额模板校验的首个客户端初始化也不落在主线程。
3. **Keystore 单进程缓存**：`KeystoreSecretStore` 仅缓存 Android Keystore 返回的 AES `SecretKey` 句柄，避免同一启动内分别恢复 API Key/PAT 时重复装载系统密钥库。API Key/PAT 明文仍不缓存；密文、GCM 参数、SharedPreferences 位置和删除机制未改变。
4. **Compose 分帧组合**：首次从 Loading 进入模型测试页时，先组合当前可见的供应商与基础站点配置；200ms 后才将测试参数、已获取模型/结果和安全说明接入 `LazyColumn`。这样阻止 LazyColumn 预取在同一首屏阶段一次性组合屏幕外长表单；延后过程不阻塞输入、不发网络请求、不丢失模型或参数。

### 13.2 本轮布局复核项

- 测试页供应商选择改为标题行右侧 48dp 触控区域的加号；普通供应商卡固定为半栏、最小高度 72dp、上下内边距 10dp，添加动作不再占据整张卡片。
- 余额总览右侧站点数固定为单行右对齐标签，标题列使用剩余宽度，消除窄屏下逐字竖排。
- 余额卡最小高度从 176dp 收紧至 132dp；可用/总额采用标签在上、数值在下的稳定列；已用圆环收至 36dp 画布 / 40dp 容器 / 4dp 描边，轨道使用 `outline` 语义色以保证选中、浅色和深色主题下可见。

### 13.3 构建、安装与冷启动复测

| 项目 | 当前结果 |
|---|---|
| Kotlin 编译 / Debug 打包 | `:app:assembleDebug` 通过 |
| Lint | `:app:lintDebug` 通过，0 errors、4 条既有非阻断警告（Gradle/Coroutines/OkHttp 可更新与 v26 adaptive-icon 提示） |
| 包名 | `com.relaytester.app.debug` |
| 当前 APK | `0.5.1-debug`，versionCode `8` |
| APK 大小 | `17,871,480` bytes |
| SHA-256 | `540552660A6BB9EC91E74BC42DADE9D7EEC4229236B27866C76CFEBDCA7D9C23` |
| 安装方式 | API 35 模拟器执行 `adb install -r` 覆盖安装；未卸载、未清空应用数据 |

设备为 `RelayTesterApi35`（Android API 35，1080 × 2400，420 dpi）。连续 5 次 `force-stop → am start -W` 冷启动 `TotalTime` 如下：

| 次数 | 1 | 2 | 3 | 4 | 5 | 中位数 |
|---|---:|---:|---:|---:|---:|---:|
| TotalTime | 1542ms | 1492ms | 1478ms | 1512ms | 1522ms | **1512ms** |

范围为 **1.478–1.542 秒**；五次启动后 `MainActivity` 均位于前台，筛选后的 `AndroidRuntime` 日志为 0。启动瞬态的 `gfxinfo` 仍会包含 Android 进程/ART/Compose 首次加载（本次 7 帧样本中的高耗时帧不应与已稳定页面的滚动帧混同）；没有位图上传慢帧，也未发生崩溃或 ANR。对于真实设备，仍建议按相同脚本复测启动体感和耗电。

### 13.4 验证边界

- 本轮没有调用真实模型列表、模型测试或余额端点，也没有读取、输出、清除或改写用户保存的 API Key/PAT。
- 新增代码只改变启动依赖创建和首屏组合时序，不改变余额模板协议、模型请求、凭据字段、保存格式或已完成的双栏/深色/大字号布局规则。
- v0.5.0 已完成的余额页有效滚动样本为 950 帧、2.32% 卡顿帧、P90 11ms、P95 17ms、P99 18ms；本次 v0.5.1 没有改变余额滚动布局。模型页的历史 `gfxinfo` 会受该模拟器中 PocketBridge 前后台抢占影响，真机或独占模拟器仍应在最终发版前复测。

---

## 14. 完整配置加密备份与迁移：v0.7.0（2026-08-30）

本节以 [CONFIG_BACKUP_IMPORT_EXPORT_V5_SPEC.md](CONFIG_BACKUP_IMPORT_EXPORT_V5_SPEC.md) 为本轮唯一实现与验收依据；第 12、13 节保留为早期版本的历史记录。

### 14.1 实施内容

1. **加密跨设备迁移格式**：新增 `.rtbackup` 版本化 JSON 信封。信封采用 PBKDF2-HMAC-SHA256（210,000 次、随机 16-byte salt）派生 AES-256 密钥，并以 AES-GCM（随机 12-byte IV、128-bit tag）加密内部配置。外层不包含可读的供应商、站点地址、模型、模板或凭据。
2. **完整配置边界**：供应商、Base URL、协议、模型、测试参数、余额模板、当前选中供应商、API Key、PAT 和可选用户 ID 会随备份迁移；测试结果、余额快照、错误和日志不会迁移。内置 `new-api` 模板总是由本应用重建，避免导入文件覆盖受维护的默认安全约束。
3. **安全导入事务**：导入先校验、解密并预览；只有再次输入密码并确认覆盖后，才创建新的 Keystore 凭据并保存新的 DataStore。若 DataStore 提交前任意步骤失败，本轮产生的 Keystore 条目会被清理，旧配置保持有效；成功后再尽力移除无引用的旧密文。
4. **共享入口和渐进披露**：模型测试与余额查询顶部栏都接入同一个“配置备份与导入”入口。导出以两次密码输入保护；导入按“选文件 → 验证密码 → 摘要 → 再次密码确认”逐步展开，避免初始弹窗一次展示全部高风险操作。
5. **性能与安全保持**：文件读写、PBKDF2/AES-GCM、Keystore 与 DataStore 操作位于后台调度器；导入/导出期间阻止初始化、模型测试和余额查询并发进行，且导入完成不会自动联网。

### 14.2 构建与模拟器结果

| 项目 | 结果 |
|---|---|
| Kotlin / Debug 打包 | `:app:assembleDebug` 离线通过 |
| Lint | `:app:lintDebug` 离线通过，0 errors、5 个非阻断 warning |
| 包名与版本 | `com.relaytester.app.debug`，`0.7.0-debug`，versionCode `10` |
| APK 大小 | `17,988,786` bytes |
| SHA-256 | `419912BD20DEA755C1C59B75F9994C542AC83291D0EA3D227A4B5CEEFB0594A3` |
| 安装 | API 35 模拟器 `adb install -r` 覆盖安装；未卸载、未清除数据 |
| 启动 | `MainActivity` 位于前台、应用进程存活；清空后 `AndroidRuntime` 无崩溃 |
| 本地流程 | 顶部入口、导出密码门槛/一致性、密码显示切换、系统文件选择器打开与取消返回路径已验证；API 35 进程内的虚构配置已通过加密、无明文、正确/错误密码、篡改拒绝与密码清零冒烟测试；未调用真实站点 API |

### 14.3 验证边界

现有模拟器包含用户原有 API Key/PAT。为防止创建用户未知密码却包含真实凭据的备份，也避免为测试而替换用户 Keystore 引用，本轮没有执行真实数据的加密导出和覆盖恢复。加密文件格式的正向与异常路径已在同一 API 35 应用进程中用完全虚构的配置验证通过；完整的 DataStore/Keystore 覆盖提交仍应在新建的无敏感测试配置或独立真机上补做。测试期间未卸载或清空应用，也未调用模型、余额或其他站点接口。

---

## 15. 紧凑界面、启动反馈与品牌标识：v0.7.2（2026-08-30）

本节以 [UI_STARTUP_POLISH_V5_SPEC.md](UI_STARTUP_POLISH_V5_SPEC.md) 为本轮设计、实现和验收依据。它取代早期版本中关于供应商卡尺寸、配置迁移入口图标和启动视觉的描述；旧节仅作为历史记录保留。

### 15.1 本轮实施内容

1. **紧凑且可读的模型测试页**：双栏供应商卡改为 `heightIn(min = 60.dp)`，以两行内容垂直居中；供应商名使用半粗体，模型数字采用强调色。接口协议与五个 Prompt 预设共用 36dp 视觉 / 48dp 触控的 `SelectablePill`，避免“回复ok”等短预设在窄屏折行。
2. **余额模板和安全提示**：模板选择菜单限制为 280dp，高度过多时内部滚动；模板名和“内置 / 自定义”标签同行。模板安全说明收敛为 HTTPS、凭据、受限模板三条独立规则，不执行任意脚本。
3. **配置迁移体验**：顶部入口由 `SettingsBackupRestore` 改为双向 `ImportExport`，可访问名称为“导入或导出配置”。弹窗标题为“配置导入与导出”，说明准确区分不含凭据的测试结果 JSON 与包含凭据、经用户密码加密的 `.rtbackup`；“导出 / 导入 / 关闭”三个 48dp 操作等宽同一行。
4. **启动恢复与交互保护**：恢复拆成 DataStore/结构阶段和 Keystore/凭据阶段；后者最多阻塞交互 1.2 秒，迟到读取只能安全回填用户未编辑的字段。列表派生计算在 `Dispatchers.Default`，网络客户端和屏幕外内容按需创建，启动不请求模型或余额。
5. **首帧交接修复**：`MainActivity` 先用原生 `FrameLayout` 绘制无敏感加载层，再创建 `ComposeView`。原生层保留到 Compose 首帧实际绘制；绘制监听器通过 `View.post` 在回调结束后注销，修复了 API 35 模拟器发现的“在 `onDraw` 中直接移除 listener”导致的 `IllegalStateException`。
6. **品牌标识重绘**：桌面图标、Android 12+ 启动面、原生加载页和顶部品牌位统一为 VectorDrawable。新图形由深靛圆角底座、蓝/紫/青绿三枚中继节点、连接线和中央验证勾组成，语义为“中继连接已通过测试”；不再使用像菜单的绿色三横线图形。日间/夜间启动背景分别为 `#F4F7FF` 与 `#0A1024`，并提供同轮廓 monochrome adaptive-icon 版本。

### 15.2 可访问性与数据边界

- `ImportExport` 图标保留“导入或导出配置”的可访问名称；顶部品牌图形仅作装饰，紧邻的 `Relay Tester` 文本承担名称语义。
- 所有配置导入/导出首页按钮仍保持不低于 48dp 的触控高度与 8dp 间距；主按钮为导出，导入和关闭是次级描边按钮。
- 启动页仅显示品牌、加载状态和进度，不显示站点、模型、Base URL、API Key、PAT 或用户 ID。
- 测试结果 JSON 继续不含 API Key/PAT/用户 ID；`.rtbackup` 会包含这些凭据，但只以用户密码加密的 PBKDF2 + AES-256-GCM 文件写出。

### 15.3 构建与模拟器验证

| 项目 | 结果 |
|---|---|
| Kotlin / Debug 打包 | 离线 `:app:assembleDebug` 通过 |
| Lint | 离线 `:app:lintDebug` 通过，0 errors |
| 包名与版本 | `com.relaytester.app.debug`，`0.7.2-debug`，versionCode `12` |
| APK | `app/build/outputs/apk/debug/app-debug.apk`，`17,786,807` bytes |
| SHA-256 | `39D53DE59688E91852659C9FC421948135AA8A986459D436AB8BA94CA5F4C974` |
| 安装 | Android API 35 模拟器执行 `adb install -r` 覆盖安装；未卸载、未清除应用数据 |
| 品牌图标 | 已在 API 35 模拟器桌面图标视觉确认新的中继节点与验证勾标识 |
| 启动稳定性 | 冷启动后 `MainActivity` 处于 top-resumed，清空本次日志后 `AndroidRuntime` 为 0 条错误 |

### 15.4 性能与验证边界

该 API 35 模拟器先前进行过应用编译优化重置，并存在系统运行时预热和 UI Automator 偶发无法取得根节点的问题。最近冷启动的系统 `TotalTime` 在该环境中达到 26 秒量级，因此不将其作为真机启动性能指标。本轮代码确保系统启动面之后有连续的非敏感原生加载反馈，并避免主线程 DataStore/Keystore/网络初始化；严格的性能 KPI 应在未被重置编译优化、没有外部负载的真机或独占模拟器重新采样。

为保护已有供应商和凭据，本轮不调用真实模型、余额或站点接口；不导入、导出、覆盖或清除用户配置。模拟器验证只覆盖安装、启动、资源渲染、Activity 存活和崩溃日志，不能替代有独立测试站点时的真实业务接口回归。

---

## 16. 紧凑控件、统一启动和品牌重绘：v0.7.3（2026-08-30）

本节以 [UI_STARTUP_POLISH_V6_SPEC.md](UI_STARTUP_POLISH_V6_SPEC.md) 为唯一的当前设计和验收依据。第 15 节记录 v0.7.2 历史实现，不能再作为供应商卡高度、导航文案、品牌外框或启动交接的现行规则。

### 16.1 界面实施

1. **供应商卡与操作位置**：两行供应商卡使用 `heightIn(min = 52.dp)`，正常字号内容预算为名称 20dp、元信息 16dp、上下各 4dp；内层垂直居中且取消元信息的额外顶部间距。供应商标题右侧为 48dp 加号和 48dp `DeleteOutline` 垃圾桶，二者之间 4dp；垃圾桶有“删除当前供应商”可访问名称。删除文字按钮不再出现在保存区，配置卡只保留“保存站点”。
2. **选择控件**：协议三项、Prompt 五项均改为 `weight(1f)` 等宽项与 8dp 间距。视觉胶囊为 36dp，点击热区为 48dp；外层 `clickable` 使用独立 interaction source 且 `indication = null`，消除了大于胶囊边界的浅色 ripple 状态层。选中态仍由描边、容器色、文字色和 RadioButton/selected 语义共同表达。
3. **余额查询一致性**：底部导航 `BALANCE` 标签改为“余额查询”，与顶部副标题统一。模板卡“安全边界”的三条规则使用 8dp 固定圆点、8dp 间距和 `tertiary` 语义色，不再以过小的文字点承担强调。
4. **无外框品牌**：新的前景 `VectorDrawable` 移除了深色方块与顶部浅色 `Surface` 包边。图形由一个可呼吸的验证环、三枚中继节点、三段连接线与中央验证勾构成；Adaptive Icon、monochrome 图标、Android 12+ Splash、原生加载页和顶部标题共同复用该图形，保证 24dp 到桌面尺寸都保持清晰。
5. **启动统一与交接兜底**：系统启动和原生加载页强制读取同一个日间/夜间 `startup_window_background`，不再按平台默认背景色推断。Animated Vector 在 680ms 内执行一次验证环呼吸。Compose 首帧会立即撤下加载层；对 API 35 图形管线延迟首帧回调的情况，增加 1.2 秒上限兜底，防止加载层永久遮挡主界面。

### 16.2 代码审查与验证

| 项目 | 结果 |
|---|---|
| Kotlin / Debug APK | 离线 `:app:assembleDebug` 成功 |
| Lint | 离线 `:app:lintDebug` 成功，0 errors |
| 版本 | `com.relaytester.app.debug`，`0.7.3-debug`，versionCode `13` |
| APK | `app/build/outputs/apk/debug/app-debug.apk`，`17,802,284` bytes |
| SHA-256 | `6C46E5550656441CE996A867003EFCB97C89C9BAF0C3EFAB1C3D50FE4C7E0989` |
| 安装与启动 | API 35 测试模拟器重启后，`adb install -r` 覆盖安装、`MainActivity` 恢复到前台成功 |
| 模型测试页 | 实测供应商两行紧凑卡、加号/垃圾桶操作组、保存区、等宽协议胶囊与无外层遮罩的样式生效 |
| 余额查询页 | 实测 Tab 与顶部标题为“余额查询”，模板安全区三枚 8dp 圆点与说明生效 |
| 崩溃检查 | 本轮清空日志后筛选 `AndroidRuntime` 为 0 条错误 |

### 16.3 测试边界

API 35 模拟器曾因系统负载和覆盖安装后的 ART 校验而出现极长系统 Splash；重启该测试模拟器后完成了实际首页、Tab 和模板安全区复核。为保护既有配置，本轮不点击模型获取、模型测试、单站点/批量余额查询、导入或导出；未读取、输出、删除或覆盖 API Key、PAT、用户 ID、站点地址或真实 API 响应。该验证证明安装、启动和局部界面交互稳定，但不替代独立测试站点上的真实网络回归。

---

## 17. 8dp 网格与发布收尾：v0.7.4（2026-08-30）

本节覆盖第 16 节的发布包信息；界面与启动的完整规范仍以 [UI_STARTUP_POLISH_V6_SPEC.md](UI_STARTUP_POLISH_V6_SPEC.md) 为准。

1. 模型测试供应商网格横纵间距从 10dp 统一为 8dp，双栏卡宽采用 `(可用宽度 - 8dp) / 2`；52dp 自适应两行卡、垂直居中、加号/垃圾桶操作组、无外溢 Ripple 胶囊保持不变。
2. 启动标题和说明使用 `strings.xml`，`RelayAppHeader` 的 `Modifier` 参数顺序符合 Compose 约定。Adaptive Icon 保留 `mipmap-anydpi-v26`，因为实际 AAPT 验证表明无版本目录不能链接应用图标；该资源限定提示作为已验证的非阻断 lint 警告保留。
3. 以 API 35 模拟器完成 `adb install -r` 覆盖安装与冷启动，`MainActivity` 进入前台；已切换并确认“余额查询”、`全部站点余额` 和 `批量查询余额` 标签，向下滚动确认“安全边界”三项规则。筛选后的崩溃日志为 0 条。整个过程不请求模型/余额/站点 API，且未导入、导出、清除或改写用户配置。
4. 当前发布版本为 `0.7.4-debug`，versionCode `14`；APK 为 `app/build/outputs/apk/debug/app-debug.apk`，大小 `17,789,085` bytes，SHA-256 为 `42EE665178078D3B3477DDEB5598E926B1AE464B0258947E6C6249AEE5D74F34`。

---

## 18. 截图回归：单一启动面与供应商卡基线居中：v0.7.5（2026-08-30）

本节以用户提供的三张启动/模型页截图为问题证据，并以 [UI_STARTUP_POLISH_V6_SPEC.md](UI_STARTUP_POLISH_V6_SPEC.md) 第 9 节为现行规则。

1. **供应商卡测量修复**：外层卡片和内部 `Box` 同时保留 `min = 52.dp`，`Box` 使用 `contentAlignment = Center`，两行 `Column` 不再依赖测量上限不确定时失效的 `fillMaxSize()`。这使名称和“协议 · 模型数”成为同一个、在卡片高度上居中的文本组；大字号仍会自然扩高。
2. **启动链路删减**：`MainActivity` 从“系统 Splash → 原生加载 FrameLayout → ComposeView”改为“系统 Splash → Compose”。旧的 `FrameLayout`、144dp 图片、加载文案、进度环、近透明 ComposeView 和 1.2 秒撤层回调不再参与运行。Android 12+ 系统 Splash 负责首帧前的唯一品牌画面。
3. **实机模拟验证**：API 35 模拟器已覆盖安装 `0.7.5-debug`（versionCode 15），冷启动后供应商卡为约 52.2dp、应用层原生启动页节点不存在、`MainActivity` 位于 top-resumed，`AndroidRuntime` 崩溃数为 0。验证未调用模型、余额或站点 API，未导入、导出、清除或改写用户配置。
4. 当前 APK 路径为 `app/build/outputs/apk/debug/app-debug.apk`，版本为 `0.7.5-debug`（versionCode `15`），大小 `17,789,085` bytes，SHA-256 为 `24714821EC30AC5EA5A69644C6732CC77FD39F148EAF914754452D81E935A890`。离线 lint 为 0 errors、4 条既有非阻断 warning。
