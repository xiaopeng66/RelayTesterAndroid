# Relay Tester Android 1.3.0

发布日期：2026-09-22

## 版本信息

- 版本号：`1.3.0`
- 版本代码：`10301`
- 提交：`待发布`
- 更新重点：支持明文 HTTP 站点、慢速中转站超时修复、批量拉取失败可定位，以及仅拉取模型模式。

## 更新内容

- **明文 HTTP**：Base URL 以 `http://` 开头时按明文请求，可直接使用仅提供 HTTP 的自建站点；未填写协议的地址仍默认使用 HTTPS。站点配置在输入明文地址时会给出风险提示。
- **超时修复**：连接、读取、写入超时随「超时（秒）」设置同步缩放，不再固定为 OkHttp 默认的 10 秒。此前慢速中转站在合法的 TLS 重协商期间会提前失败，表现为「超时但实际可用」。
- **批量拉取**：批量拉取模型失败时会逐个列出失败的供应商名称与原因，区分「全部失败」与「部分成功」，空模型目录单独说明。
- **模型名复制**：测试结果卡片与已拉取模型列表的模型名旁新增复制按钮，可一键复制模型名。
- **高级测试**：模型卡片新增目录刷新按钮，可核对来源是否仍在该站点的模型目录中；站点已下架的来源标注「已不存在」，并禁止再对其发起测试；对已不存在的来源，测试按钮改为删除按钮，单击即移除该来源，不再弹出二次确认。
- **仅拉取模型**：站点配置新增「禁止测试（禁测活网站请勾选）」开关，开启后该供应商只允许刷新模型列表，不会发送任何连通性测试请求，适合按次计费或限流严格的站点。卡片左上角以红色标记提示该状态。
- **站点删除**：删除入口统一收敛到站点配置弹窗卡片右上角，二次确认后删除该供应商；仅剩一个供应商时该按钮禁用。
- **界面布局**：供应商卡片高度固定，双栏始终等高；修复了长供应商名被右上角按钮遮挡的问题。
- **结果卡片对齐**：测试结果卡片的状态圆点移入模型名行内并随之垂直居中；「可用 / 进行中 / 待测」在卡片右侧垂直居中显示，「失败」与模型名行对齐、「详情 / 收起」与测试结果行对齐，两者保持同一水平位置；快捷筛选卡片的删除叉号改为卡片内垂直居中。

## 修复

- 修复仅提供 HTTP 的中转站无法配置的问题。
- 修复余额查询对 HTTP 站点始终失败的问题：端点校验改为要求协议、主机、端口与所配置站点完全一致，既允许明文站点查询自身余额，也仍然阻止模板把请求与凭据指向其他站点。
- 修复慢速站点在配置的超时时间之前就报超时的问题。
- 修复批量拉取模型失败时无法判断是哪些供应商失败的问题。
- 修复删除当前正在编辑的供应商后配置弹窗残留的问题。
- 修复移除来源后「已不存在」标记残留的问题：来源被删除时同步清理其失效标记，避免同名来源被误判。
- 修复删除模型条目后其失效标记残留的问题。
- 修复移除模型条目最后一个来源后留下无来源条目的问题：最后一个来源被移除时整个条目一并删除（与存储层「条目至少需要一个来源」的规则保持一致），若该条目正在编辑则同时重置编辑草稿，避免草稿指向已删除的模型。

## 验证

- **单元测试**：`testDebugUnitTest` 通过，共 19 个用例，覆盖地址解析、明文识别、仅拉取标记的持久化与旧配置兼容、余额端点解析与越权拦截、模型来源标识。
- **红绿验证**：临时回退余额端点修复后对应用例立即失败，恢复后通过，确认用例能够捕获该缺陷。
- **构建任务**：`compileOptimizedKotlin`、`testDebugUnitTest`、`lintVitalOptimized`、`assembleOptimized` 均通过。
- **运行验证**：在 API 35 模拟器上使用本机明文 HTTP 服务完成模型拉取，确认明文请求可达；验证了站点删除的完整流程、仅拉取模式的卡片标记与测试入口禁用、双栏卡片等高；并通过 UI 层级 dump 复核「禁止测试（禁测活网站请勾选）」为单行渲染（标签高度 52px，无换行、无截断）。
- **回归范围**：覆盖模型测试、高级测试、余额查询、站点配置与导入导出；未调用真实站点的模型或余额接口，未导入或清除用户数据。

## 下载

- 文件：`RelayTester-v1.3.0-android.apk`
- 大小：`见 Release 附件`
- SHA-256：`见 Release 附件说明`
- 说明：R8 优化、debug 签名的小安装包，包名 `com.relaytester.app.debug`，用于体验和覆盖同签名调试包。

