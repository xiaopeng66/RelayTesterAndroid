# Relay Tester Android 1.1.0 开发规范

发布日期：2026-08-31

## 目标

本版本修复模型列表筛选不便、余额结果退出后丢失，以及余额供应商卡在长金额下信息拥挤的问题。保留既有页面架构、Material 3 色彩、双栏供应商布局与网络安全边界。

## 需求与验收

| 范围 | 行为 | 验收标准 |
| --- | --- | --- |
| 文本筛选 | `|` 为 OR 分隔符，兼容全角 `｜` | 输入 `gpt|claude` 时，名称包含任一词的模型均显示 |
| 快捷筛选 | 每个供应商可添加、删除多个模型词；点击可多选 | 快捷筛选词与文本词合并为 OR，删除后不再参与匹配 |
| 测试结果搜索 | 使用同一 OR 规则 | 结果搜索与获取后的模型列表行为一致 |
| 开始测试 | 使用当前文本词和已选快捷词确定目标 | 不匹配的模型不会进入本轮请求 |
| 余额结果 | 成功快照退出后默认保留 | 重启应用后所有有效供应商卡和当前详情恢复上次成功结果 |
| 快照失效 | 站点删除、模板切换/更新/删除后清理对应快照 | 不显示来自旧模板或已删除站点的余额 |
| 余额卡 | 圆环位于右上、已选位于右下 | 长余额数字使用完整主列宽度，不被圆环挤压 |

## 交互设计

### 模型筛选

1. “模型名过滤（OR）”与“搜索测试结果（OR）”均提示 `gpt|claude` 示例。
2. 空片段会忽略，例如 `gpt||claude|` 等同于 `gpt|claude`。
3. 过滤词不区分大小写；最多保存 16 个快捷词，每个词最多 128 个字符。
4. “快捷筛选”使用可换行的 InputChip 集合，间距为 8dp，点击切换选中，关闭图标删除定义；添加图标的点击热区为 48dp。
5. 快捷筛选定义属于供应商的测试设置，需要随“保存站点”、拉取模型、开始测试或配置导出一并落盘。当前选中状态只影响当前页面会话，不跨供应商或跨启动保留。

## 数据与安全

### 本地 DataStore

`SupplierStoreState` 新增 `balanceSnapshots` 字段。每个快照仅包含：

- 供应商与模板标识、模板名称；
- 原始可用/已用/总额、单位、币种和套餐名；
- 换算除数、查询时间和延迟。

快照不包含 API Key、余额 PAT、用户 ID、请求 URL、请求头或响应原文。套餐名与单位等可变文本会限制长度，避免异常上游响应膨胀本地配置。

读取时会验证数值有限性、换算除数和供应商/模板引用。损坏或无效条目会被跳过，不影响其余供应商启动。

### 配置备份

快捷筛选定义属于测试设置，会进入现有密码加密的 `.rtbackup`。余额快照属于易失展示缓存，不进入备份；这样跨设备导入不会把旧站点余额误显示为新设备的最新查询结果。

## 实施边界

- 不发起任何额外的模型或余额 API 请求；恢复只读取本机 DataStore。
- 查询成功后，先更新界面，再在 `Dispatchers.IO` 写入快照。即使本地保存失败，刚获取的余额仍显示，并会给出明确提示。
- 模板、供应商和余额凭据仍遵循既有“先落盘配置、后清理旧 Keystore 引用”的事务顺序。
- 余额快照与凭据无关，不能替代站点 API 的实时余额；界面仍展示上次查询时间。

## 布局规范

- 双栏余额卡保持固定 124dp 高度，12dp 内边距。
- 标题行左侧显示供应商名，右侧固定 32dp 查询状态或 32dp 圆环。
- 可用与总额位于全宽主列，数值使用单行省略保护，但不再与圆环共享同一行宽度。
- 底部状态文本占左侧弹性宽度；`已选` 独立锚定在右下。
- 状态圆环为 32dp 绘制区域、3.5dp 描边，视觉容器 36dp；色彩继续使用主题语义色，兼容深色与浅色模式。

## 验证清单

1. 输入 `gpt|claude`、`gpt｜claude`、空串和重复词，检查获取列表、开始测试目标和结果搜索。
2. 添加多个快捷词，分别单选、多选、删除；切换供应商后确认定义与选中状态符合范围。
3. 使用安全的测试快照验证成功余额写入后重启恢复；切换模板、删除模板和删除供应商后重启确认快照已清除。
4. 在小屏和长余额文本下检查双栏卡：圆环仅在右上、已选仅在右下，数值不重叠。
5. 运行 `lintDebug`、单元测试、Debug/optimized/release 构建；在 API 34 模拟器覆盖安装优化包，检查冷启动、两页导航和崩溃/ANR 缓冲。

## 发布

- 版本：`1.1.0`
- versionCode：`10100`
- 发布标签：`v1.1.0`
- GitHub Release 仅上传通过模拟器验证的 `app-optimized.apk`；未签名 release APK、签名材料、本地配置、截图、备份和任何密钥继续排除。
