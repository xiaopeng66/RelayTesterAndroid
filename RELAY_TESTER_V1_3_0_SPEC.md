# Relay Tester Android 1.3.0 开发规范

发布日期：2026-09-21

## 目标

本版本解决七项用户反馈问题：明文 HTTP 站点无法使用、批量拉取模型失败无法定位、模型名无法复制、高级测试缺少目录刷新与下架检测、供应商缺少直接删除入口、慢速中转站被误判超时，以及缺少“仅拉取模型、禁止连通性测试”的开关。

保留既有页面架构、Material 3 色彩、双栏供应商布局与默认 HTTPS 的安全边界。

## 需求与验收

| 编号 | 范围 | 行为 | 验收标准 |
| --- | --- | --- | --- |
| R1 | 明文 HTTP | 用户显式输入 `http://` 时按明文请求；无 scheme 仍默认 HTTPS | 输入 `http://host/v1` 保持明文并可请求；输入 `host/v1` 解析为 `https://host/v1` |
| R2 | 批量拉取失败明细 | 每个失败供应商记录名称与原因，汇总文案逐个列出 | 3 个供应商中 1 个失败时，提示包含失败供应商名与原因 |
| R3 | 模型名复制 | 结果卡片与已拉取模型列表的模型名旁提供复制按钮 | 点击复制后剪贴板内容等于该模型名 |
| R4 | 目录刷新与下架检测 | 高级测试模型卡片支持一键刷新；下架模型标注“已不存在” | 刷新后站点已下架的来源显示“已不存在”，且不允许再对其发起测试 |
| R5 | 供应商删除 | 站点配置卡片右上角提供删除按钮，二次确认后删除该供应商 | 删除非当前供应商不改变当前选中；仅剩一个供应商时删除按钮禁用 |
| R6 | 慢速 API 超时 | 连接/读取/写入超时随用户配置的超时时间缩放，而非 OkHttp 默认 10 秒 | 配置 20 秒超时的站点，其合法 TLS 重协商不再提前失败 |
| R7 | 禁止测试 | 供应商可标记为“仅拉取模型”；测试入口禁用并标注 | 开启后“开始测试”不可点，卡片显示“仅拉取模型”徽章，强制运行也不发送任何请求 |

## 交互设计

### R1 明文 HTTP

1. 地址解析统一收敛到 `RelayBaseUrl`。规则：`https://` 前缀原样保留；`http://` 前缀原样保留；其余一律补 `https://`。
2. 明文是用户显式选择的结果，不做任何隐式降级：只有用户手动输入 `http://` 才会走明文。
3. 网络安全配置将 `cleartextTrafficPermitted` 放开为 `true`，否则 Android 会在 OkHttp 之前直接阻断明文请求。
4. 校验失败文案由“必须是有效的 HTTPS 地址”改为“必须是有效的 HTTP(S) 地址”，与新的能力一致。

### R2 批量拉取失败明细

1. 批量拉取并发进行，每个供应商的结果写入线程安全列表，避免并发写丢条目。
2. 汇总分三种情况：全部成功、全部失败、部分成功。三种情况都逐个列出失败供应商名与原因。
3. 空目录属于“请求成功但无可用模型”，单独给出原因，不计入请求失败。

### R3 模型名复制

1. 复制按钮位于模型名右侧，使用 `ContentCopy` 图标，热区 48dp。
2. 覆盖两处：测试结果卡片、已拉取模型列表。
3. 复制走系统剪贴板，不写入任何日志或导出内容。

### R4 目录刷新与下架检测

1. 高级测试的模型卡片新增刷新按钮，逐来源与站点实时目录比对。
2. 比对结果分两类：站点目录无法访问（保持原状态，提示站点不可用）、站点目录可达但缺少该模型（标记“已不存在”）。
3. 标记为“已不存在”的来源不可再发起测试；若一个模型下所有来源都不可测试，该模型的行内运行按钮禁用。
4. 供应商被删除时，其来源一并视为不可用。

### R5 供应商删除

1. 删除按钮位于卡片右上角、编辑按钮左侧，避免误触首先命中破坏性操作。
2. 二次确认弹窗复用既有实现，文案包含供应商名称。
3. 删除按 id 执行，不再依赖“当前选中”，因此删除非当前供应商不会切换当前站点。
4. 仅剩一个供应商时按钮禁用，防止出现零供应商的不可用状态。

### R7 禁止测试

1. 站点配置表单在 API Key 提示下方新增“仅拉取模型（禁止连通性测试）”复选框。
2. 开启后供应商卡片显示“仅拉取模型”徽章，卡片高度相应增加以容纳徽章。
3. “开始测试”按钮禁用并显示“已禁止测试”。
4. 强制运行（例如并行测试路径）也不发送请求：统一发布 `TESTING_DISABLED` 结果，说明该供应商仅允许拉取模型。
5. 拉取模型不受影响，这是该模式的唯一用途。

## 数据与安全

### 领域模型

`SupplierProfile` 新增 `isTestingDisabled: Boolean = false`，默认关闭，保证既有配置行为不变。

`ErrorKind` 新增 `TESTING_DISABLED("已禁止测试")`，用于汇总与筛选，不引入新的敏感字段。

### 本地 DataStore

`SupplierStore` 序列化与反序列化新增 `isTestingDisabled`。读取使用 `optBoolean(..., false)`，旧配置缺少该字段时按可测试处理，不需要迁移。

### 配置备份

`ConfigurationBackupSupplier` 新增 `isTestingDisabled`，导出写入、导入读取，同样以 `false` 为默认值。这样跨版本导入旧备份不会误禁测试。

### 网络安全边界

`network_security_config.xml` 放开明文，但默认行为不变：

- 无 scheme 的地址仍补 `https://`，即所有既有配置继续走 TLS。
- 明文仅在用户显式输入 `http://` 时发生。
- API Key、PAT、用户 ID 仍不进入测试结果与导出；明文请求只影响传输层，不改变脱敏规则。

## 超时实现

原实现只对请求应用 `callTimeout`，连接、读取、写入仍使用 OkHttp 默认值（各 10 秒）。慢速中转站在合法 TLS 重协商期间会超过 10 秒的连接窗口，从而在用户配置的超时时间之前就失败，表现为“超时但实际可用”。

修复方式：按请求构建客户端，把 `connectTimeout`、`readTimeout`、`writeTimeout`、`callTimeout` 全部设置为用户配置的超时时间加 5 秒宽限。`callTimeout` 继续作为整体上限。

## 实施边界

- 不新增任何依赖；不改变既有协议实现（Chat Completions / Responses / Anthropic Messages）。
- 不发起超出用户操作的额外请求：目录刷新只调用 `GET /models`。
- 禁止测试的供应商不产生任何出站测试请求，包括批量路径。
- 删除供应商只删除本地配置，不向站点发起请求。

## 验证要求

1. `testDebugUnitTest` 通过，新增回归用例覆盖地址解析、明文识别、标记持久化、旧配置兼容、来源标识与多来源保留。
2. 红绿验证：临时回退修复后，对应用例必须失败，证明用例确实能捕获缺陷。
3. `lintVitalOptimized`、`assembleOptimized` 通过。
4. 模拟器实机验证七项行为，重点验证明文站点、删除非当前供应商、禁止测试不发出请求。
