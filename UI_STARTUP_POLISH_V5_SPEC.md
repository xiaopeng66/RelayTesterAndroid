# Relay Tester Android：紧凑卡片、迁移入口与启动流畅性优化规范（v0.7.2）

**日期：** 2026-08-30  
**状态：** 已实施；最终离线构建与 lint 通过，已完成 API 35 覆盖安装、品牌图标与启动存活复核。  
**范围：** 模型测试页供应商/Prompt、余额模板选择与安全提示、配置备份入口、品牌图标及启动阶段性能；不改变站点请求协议，不请求真实模型或余额接口。

---

## 1. 本轮问题与目标

用户反馈当前版本仍存在以下问题：

1. 模型测试的双栏供应商卡只放两行文字，却仍有明显底部留白；供应商名和模型数量缺少视觉层级。
2. 测试 Prompt 的“快速选择”与“接口协议”使用了不同的视觉语言，显得零散。
3. 站点配置的“不会导出”文案容易让人误解完整配置备份不会携带凭据。
4. 余额查询模板的下拉菜单在模板多时可能过高；内置/自定义提示占用了第二行。
5. 模板区域的安全边界不够醒目、具体且可扫描。
6. 配置备份入口图标语义偏“恢复设置”，而不是“导入/导出”；首页三个操作没有放在同一行，说明文字也未把两种导出类型区分清楚。
7. 应用首开虽然已将 I/O 转到后台，但在配置恢复完成和延后内容组合时仍有卡顿感。
8. 当前桌面与系统启动图标是绿色圆角卡片加三条横线，容易被理解为“列表 / 菜单”，不能传达 Relay Tester 的中继连接与测试用途；浅色系统启动面也缺少应用内视觉延续。

本轮目标是：保持 48dp 触控热区、深色/浅色对比度和 1.3× 字号适配的前提下，压缩卡片、统一选择控件、消除导出文案歧义，并进一步将首开阶段的非必要计算移出可交互首帧。同时以一个可缩放的原生矢量标识统一桌面图标、系统启动面、应用内加载层和顶部品牌位。

---

## 2. 文案与安全真相表

以下定义为唯一准则，所有用户可见提示必须与之保持一致：

| 用户动作 | 是否包含 API Key / PAT / 用户 ID | 文案要求 |
|---|---:|---|
| “导出测试结果 JSON” | 否 | 明确写“仅测试结果，不含密钥或余额凭据”。 |
| “配置备份与迁移”导出的 `.rtbackup` | 是，加密 | 明确写“包含 API Key、PAT 和用户 ID，受备份密码加密保护”。 |
| Android Keystore 本机保存 | 是，设备加密 | 说明“本机加密保存”，不能把它描述成“永不导出”。 |

站点配置卡中的旧提示“密钥不会进入……导出文件”改为：

> 密钥由 Android Keystore 加密保存。测试结果 JSON 不含密钥；加密配置备份会包含密钥。

测试结果区的导出按钮改为“导出测试结果 JSON”，辅助提示同样只指向测试结果，不泛指配置备份。

---

## 3. 模型测试页设计

### 3.1 供应商卡的尺寸计算

供应商卡只包含两行：

- 名称：`titleSmall`，改为 `SemiBold`，正常行高 20dp；
- 元信息：`labelMedium`，行高 16dp；名称与元信息间距 2dp；
- 上下内边距各 6dp。

正常字号下内容高度为 `20 + 2 + 16 + 6 + 6 = 50dp`。卡片最小高度设为 **60dp**，由垂直居中布局提供上下各约 5dp 的呼吸空间；这比原 68dp 更贴合实际内容。

在大字号下卡片使用 `heightIn(min = 60.dp)` 而非固定高度：若两行内容超过 60dp，卡片自然增加高度，禁止裁剪或压缩文字。

### 3.2 文字层级

- 供应商名称：未选中使用 `onSurface`，选中使用 `onSecondaryContainer`，`SemiBold`；
- “协议 · ”：`onSurfaceVariant`；
- 模型数字：`primary` + `SemiBold`；“个模型”仍使用次级文字。数字颜色只加强层级，完整文字仍保留，不能只用颜色表达含义。

### 3.3 Prompt 与协议共用选择控件

“接口协议”和“快速选择”统一使用同一套紧凑 `SelectablePill`：

- 视觉：36dp 高的 Material 容器、同一圆角、描边和选中容器色；
- 触控：外层始终 48dp 高；
- Prompt 固定为同一行，五项使用最小 48dp 宽、6dp 间距、11sp 标签，在 360dp 宽 / 1.3× 字号下不换行；
- 控件具备 `RadioButton` 语义、选中状态与完整 TalkBack 描述。

---

## 4. 余额模板与安全边界

### 4.1 模板选择

1. 下拉触发器显示“余额查询模板”标签、当前模板名和右侧箭头，保持 48dp 热区。
2. 菜单最大可见高度为 280dp；模板多时内部垂直滚动，不能无限撑高页面。
3. 每一项单行：左侧模板名，右侧紧凑状态标签“内置”或“自定义”；名称过长尾部省略，状态标签不换行。
4. 当前选中项同时有勾选图标/文字与容器色，不能仅依赖颜色。

### 4.2 安全边界

在模板卡底部保留三条同样格式的强调说明，每条独立一行，使用 `•` 和 `tertiary` 语义色标记：

- **HTTPS：** 只能访问当前站点的 HTTPS 地址。
- **凭据：** API Key / PAT 仅在请求时注入，不写入模板或日志。
- **模板：** 仅支持受限 HTTP 与 JSON 映射，不执行脚本或代码。

不再显示大段占位符介绍；占位符详情仍仅在模板编辑页出现。

---

## 5. 配置导入与导出首页

### 5.1 入口

顶部栏图标从 `SettingsBackupRestore` 改为 `ImportExport`，可访问名称改为“导入或导出配置”。图标传达双向文件迁移，视觉图标 24dp、触控区域 48dp。

### 5.2 首页信息层级与文案

标题改为“配置导入与导出”。内容只保留三段简洁说明：

1. `加密备份会迁移供应商、模型、参数和余额模板。`
2. `它包含 API Key、PAT 和用户 ID；测试结果 JSON 不包含这些凭据。`
3. `密码不会保存。遗失后无法恢复备份内容。`

### 5.3 三按钮同一行

底部操作必须在同一行，顺序为：

```text
[ 导出 ] [ 导入 ] [ 关闭 ]
```

- 三个按钮均使用 `weight(1f)`、最小高度 48dp、间距 8dp；
- “导出”为主按钮；“导入”和“关闭”为描边次按钮；
- 使用短标签避免 360dp 窄屏、中文大字号下折行；
- 按钮不附加图标，标题图标已经给出语义，避免三按钮横排时拥挤；
- 导出/导入/关闭都保留文字，不能只使用图标。

### 5.4 品牌图标与启动视觉

1. **图形语义：** 新标识为深靛色圆角底座、三枚青蓝/紫/青绿中继节点、连线与居中的成功勾。节点和连线表达“中继连接”，勾表达“模型测试已验证”；不再使用三条横线这一容易被误解为菜单的符号。
2. **实现方式：** 使用 Android `VectorDrawable`，不依赖模糊位图；同一前景矢量用于 adaptive launcher icon、Android 12+ 系统启动图标和应用内原生加载页。单色版本保留相同轮廓，供 Android 系统主题图标使用。
3. **色彩：** 自适应图标底色为深靛 `#111C3B`；日间启动背景为浅蓝灰 `#F4F7FF`，夜间为深蓝黑 `#0A1024`。节点色仅提供品牌识别，测试成功仍以文字和状态表达，不能只靠颜色。
4. **启动交接：** 系统启动面先展示同一品牌标识；进入 Activity 后原生加载层以 76dp 标识、标题、非敏感说明和 32dp 靛色进度指示承接。加载层在 Compose 第一帧完成前持续保留。
5. **应用内一致性：** 顶部标题左侧的品牌位重用同一前景矢量；品牌图形是装饰，因相邻的 `Relay Tester` 文本已提供名称，`contentDescription` 设为 `null`，不重复干扰 TalkBack。

---

## 6. 启动流畅性方案

### 6.1 两阶段恢复

启动恢复拆成两个阶段：

1. **结构阶段（首屏优先）**：在 `Dispatchers.IO` 读取 DataStore、解析供应商/模板、确定当前供应商；发布供应商网格和基础表单，不等待 API Key/PAT 解密。
2. **凭据阶段（后台补齐）**：继续在 `Dispatchers.IO` 从 Keystore 读取当前供应商 API Key/PAT。最多阻塞交互 **1.2 秒**；若读取仍未完成，安全地释放界面，后台完成后只合并用户尚未修改的凭据字段。未编辑的空白字段绝不删除现有密钥，用户手工输入的新值也绝不被迟到读取覆盖。

首屏在结构阶段完成后即可滚动与查看配置；不预取模型、余额或网络客户端，也不显示或缓存凭据明文。

### 6.2 首帧后计算与组合

### 6.2 原生首帧加载反馈

Android 12 及更高版本会先显示系统启动面。系统将控制权交给 Activity 后，应用先绘制不依赖 Compose、DataStore、Keystore 或网络的原生加载层：

```text
正在准备 Relay Tester
正在加载本地配置…
```

- 原生层先完成至少一次绘制，再开始创建 `ComposeView`；
- 原生层会持续保留到 Compose 已经实际绘制第一帧才移除，避免慢速首次组合时出现空白或回退到静态启动面；
- 绘制监听器在下一轮主线程消息中注销，不能在 `onDraw` 回调内直接注销，以免触发 Android 的 `IllegalStateException`；
- 这只改善冷启动时的可感知反馈，不改变系统启动面的最短显示时间，也不把模拟器的运行时预热时长伪装成真实设备性能。

### 6.3 首帧后计算与组合

- 模型过滤、排序、结果列表派生等与首屏无关的列表计算转至 `Dispatchers.Default`；
- 首帧只组合供应商网格和站点配置；测试参数、已获取模型/结果、安全说明在第一帧稳定后再逐步加入；
- 延后内容不使用整卡高度动画，不阻塞用户输入，不发起网络请求；
- 保持 LazyColumn 稳定 key，批量测试状态发布继续节流。

### 6.4 不引入的方案

- 不降低 Keystore 安全性，不将 API Key/PAT 改为明文缓存；
- 不在启动时请求模型、余额或启动网络客户端；
- 不新增无法离线解析的依赖；Baseline Profile 在真机采样后另行评估。

---

## 7. 实施范围

| 文件 | 修改 |
|---|---|
| `TesterScreen.kt` | 60dp 自适应供应商卡、名称/模型数层级、共享选择 Pill、Prompt 单行、导出文案和站点密钥提示。 |
| `BalanceScreen.kt` | 受限高度模板菜单、模板项同行内置/自定义标签、三条安全边界。 |
| `RelayAppHeader.kt` | `ImportExport` 图标与准确可访问名称。 |
| `ConfigurationBackupDialog.kt` | 精简说明、三按钮同一行、准确的备份范围表述。 |
| `TesterViewModel.kt` 与相关 UI state | 两阶段启动恢复、后台派生列表计算、凭据水合期间的保护。 |
| `MainActivity.kt` | 原生首帧加载层、Compose 首帧绘制后再撤下加载层，以及安全的绘制监听器交接。 |
| `ic_launcher_foreground.xml`、`ic_launcher_monochrome.xml`、`colors.xml` | 新的中继测试矢量标识、主题图标轮廓和日间/夜间启动色。 |
| `DEVELOPMENT_IMPLEMENTATION_SPEC.md`、`UI_REDRAW_SPEC.md`、`README.md` | 加入实施记录、文案边界和验证结果。 |

---

## 8. 验收标准

| 验收项 | 结果 |
|---|---|
| 60dp 供应商卡、名称/数字层级、协议与 Prompt 统一 Pill | 已实施；供应商卡使用 `heightIn(min = 60.dp)`，文字按照语义色与半粗体分层。 |
| 模板菜单与安全边界 | 已实施；菜单最大 280dp，状态标签同行，安全规则为三条独立说明。 |
| 导入/导出入口及首页 | 已实施；`ImportExport` 为 24dp 视觉图标 / 48dp 触控入口，首页为等宽的“导出 / 导入 / 关闭”。 |
| 品牌图标与启动视觉 | 已实施；API 35 模拟器桌面图标已确认使用新的中继测试标识，启动资源和应用内加载层均引用同一矢量。离线 build/lint 通过，应用启动后 `MainActivity` 位于前台且 `AndroidRuntime` 为 0 条错误。 |
| 文案边界 | 已复核；测试结果 JSON 不含凭据，加密 `.rtbackup` 包含凭据且由用户密码保护。 |
| 构建与启动 | 已复核；`lintDebug` 与离线 `assembleDebug` 通过，API 35 覆盖安装、`MainActivity` 前台和 `AndroidRuntime` 0 错误通过。 |
| 模拟器性能边界 | 该 API 35 模拟器经历过包编译优化重置且系统负载较高，最近一次冷启动 `TotalTime` 为 26.767 秒；该数值不作为真机性能指标。系统启动面到 Activity 的交接受系统/ART 预热影响，原生加载层用于保证交接后的可见反馈。 |
| 数据保护 | 已遵守；没有调用真实模型或余额接口，未清空数据、未导出或导入用户配置，也未在日志或测试截图输出 API Key、PAT、用户 ID。 |
