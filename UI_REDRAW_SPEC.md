# Relay Tester Android UI 重绘规范（v4）

> 本文件描述视觉与交互规则；实施顺序、数据边界和最终验证记录以 [DEVELOPMENT_IMPLEMENTATION_SPEC.md](DEVELOPMENT_IMPLEMENTATION_SPEC.md) 与 [STARTUP_LAYOUT_PERFORMANCE_OPTIMIZATION_SPEC.md](STARTUP_LAYOUT_PERFORMANCE_OPTIMIZATION_SPEC.md) 为准。
>
> **当前覆盖规则（v0.7.5，2026-08-30）：** [UI_STARTUP_POLISH_V6_SPEC.md](UI_STARTUP_POLISH_V6_SPEC.md) 是供应商卡、站点操作、选择控件、余额查询命名、品牌与启动交接的唯一现行规范。本文件中标为 v0.7.4 及更早版本的尺寸、导航名称或 Logo 描述仅保留为历史记录，不能作为新的实现依据。

## 0. v0.7.5 当前界面基线

以下规则覆盖本文件中任何冲突的早期表述：

- 模型测试供应商卡为 `heightIn(min = 52.dp)` 的双栏两行卡。卡内使用同样最小高度的 `Box(contentAlignment = Center)` 承载文本组，不能依赖最大高度不受限的 `fillMaxSize()`；名称和元信息因此在卡片可用高度严格居中。名称使用半粗体，模型数量使用主色强调。
- “供应商”标题右侧依次为 48dp 加号与 48dp 错误色 `DeleteOutline` 垃圾桶，间隔 4dp；保存区只有“保存站点”，不再出现删除文字按钮。
- 协议三项和 Prompt 五项均为 36dp 视觉胶囊、48dp 点击热区、等权宽度与 8dp 间距。外层没有会溢出胶囊的 Ripple 覆盖层；选中状态由控件内部描边、容器色、文字色和 RadioButton 语义共同表达。
- 底部 Tab 与页面功能名称均为“余额查询”；安全边界每条使用 8dp `tertiary` 圆点和 8dp 文字间距。
- 桌面图标、顶部品牌位和 Android 12+ 系统启动面共用无外框的中继验证 VectorDrawable。系统 Splash 是唯一的启动表面，验证环为一次 680ms 呼吸；Compose 首帧就绪后由平台直接交接，禁止再添加应用内加载层、加载标题、进度环或人为 1.2 秒遮罩兜底。

## 1. 目标与范围

本规范用于将 Relay Tester 重绘为一款**专业、克制、适合移动端操作的开发者工具**。目标不是堆砌装饰，而是让用户能在手机上快速完成三件事：配置站点、批量验证模型、确认账户余额。

本轮已落地的界面改造包括：统一品牌栏、双栏供应商选择、获取后纵向待测模型列表、余额总览网格、余额凭据隔离、Prompt 快捷预设、余额套餐与总额展示、稳定的页内返回行为。后续页面重构必须遵守本文件的布局、状态与动效规则。

## 2. 设计方向

### 产品气质

- 定位：开发者 / 运维工具，不做消费级社交界面。
- 风格：深色优先、信息密度中等、可扫描性优先。
- 情绪：可信、安静、精确；成功状态用克制的绿色，错误仅在需要行动时使用红色。
- 图标：只使用 Material Symbols / Material Icons；不使用 Emoji 作为功能图标。

### 主题令牌

| 语义 | 深色值 | 浅色值 | 使用位置 |
| --- | --- | --- | --- |
| App 背景 | `#020617` | `#F8FAFC` | 页面底层 |
| 主表面 | `#0E1223` | `#FFFFFF` | 顶栏、普通容器 |
| 抬升表面 | `#1A1E2F` | `#F1F5F9` | 重点卡片、结果卡 |
| 主文字 | `#F8FAFC` | `#0F172A` | 标题、主要数据 |
| 次级文字 | `#CBD5E1` | `#475569` | 说明、辅助信息 |
| 主操作 | `#F8FAFC`（深色）/ `#0F172A`（浅色） | 同左 | 开始测试、查询余额 |
| 成功 | `#4ADE80` | `#15803D` | 可用、已保存、健康状态 |
| 警告 | `#FBBF24` | `#B45309` | 即将耗尽、需要注意 |
| 错误 | `#FCA5A5` | `#B91C1C` | 可操作的错误与失败 |

所有正文和背景组合必须达到至少 4.5:1 的对比度；状态不能只依赖颜色，必须有文字标签。

### 排版与间距

- 正文：系统 Sans Serif，14sp / 20sp；正文不低于 12sp。
- 数值：使用等宽或字体特性接近等宽的风格，便于比较延迟、额度、HTTP 状态。
- 标题：页面 22sp，卡片标题 16sp，字段分组 14sp。
- 间距按 4dp 网格：卡片内边距 16dp、卡片间距 16dp、紧密元素 8dp。
- 所有可点区域最小 `48 × 48dp`，相邻可点元素至少间隔 8dp。

## 3. 信息架构

底层导航只保留两个一级入口，避免在小屏上分散注意力：

```text
Relay Tester
├── 全局操作：配置导入与导出
├── 模型测试
│   ├── 双栏站点选择
│   ├── 站点连接配置
│   ├── 测试参数（Prompt、模型筛选）
│   ├── 高级设置（折叠）
│   ├── 已获取模型 / 测试结果（同一纵向列表）
│   └── 测试后结果与导出
└── 余额
    ├── 全部站点双栏余额卡
    ├── 余额查询凭据（PAT / 用户 ID）
    ├── 余额结果
    ├── 查询模板选择
    └── 模板编辑器（页内二级页面）
```

“配置导入与导出”是两个一级页面共用的全局操作，不增加第三个底部导航目的地。它位于顶部栏右侧，使用 `ImportExport` 双向迁移图标、视觉 24dp 与触控 48dp；图标控件的可访问名称为“导入或导出配置”。

## 4. 页面重绘细则

### 4.1 模型测试页

1. 顶部保留应用名和一句功能说明，避免大面积装饰性 Banner。
2. “供应商”使用固定双栏卡片；标题行右侧放置视觉 24dp、触控区 48dp 的“添加供应商”加号按钮，添加动作不得占用网格卡片。单个供应商也保持左侧半栏；普通卡使用 `heightIn(min = 60.dp)`、上下内边距 6dp，名称半粗体，模型数字使用强调色。选中项有容器色、边界/状态与 selected 语义；主流程不得依赖横向滚动。
3. 站点配置卡只放连接信息与 API Key；密钥说明必须准确区分导出范围：“密钥由 Android Keystore 加密保存。测试结果 JSON 不含密钥；加密配置备份会包含密钥。”
4. 测试参数卡中，`测试 Prompt` 下方必须有五个预设：`ping`、`Hi`、`Say OK`、`1+1=?`、`回复ok`。它与接口协议共用带描边的 `SelectablePill` 视觉系统：视觉容器 36dp、触控热区 48dp；五项在 360dp 宽 / 1.3× 字号下一行显示，不使用横向滚动。
5. 高级参数默认折叠。展开/收起直接完成，避免整张大卡片的高度动画和滚动测量波动。
6. 主按钮放在同一行：次按钮“获取模型”，主按钮“开始测试”；运行时主按钮替换为红色“取消测试”。
7. 点击“获取模型”后，模型必须直接出现在主 `LazyColumn` 中的纵向待测卡片里，状态为“已获取，尚未开始测试”。开始测试后，同一模型 key 的卡片原位显示等待、可用或失败状态及测试数据。
8. 汇总、筛选、重测、复制和导出仅在开始测试后出现；获取模型但尚未测试时不显示这些操作。
9. 首次从 Loading 进入模型测试页时，先提交供应商和基础站点配置；屏幕外的测试参数、模型列表/结果与安全说明在首个内容帧后补齐。该渐进组合仅拆分渲染负载，不能显示会让用户误以为数据丢失的空状态，也不能阻塞任何已可见输入。

### 4.2 余额页

余额页的顺序应按“全部站点 → 当前站点凭据 → 查询详情 → 模板”组织：

1. 全部站点：标题右侧站点数必须是最小 56dp、单行、右对齐的标签，不得逐字竖排或横向滚动。使用固定双栏卡片展示每个供应商；卡片最小高度 132dp，显示可用、总额、已用圆环与状态。用量圆环为 36dp 画布 / 40dp 容器 / 4dp 描边，轨道采用可见的 `outline` 语义色；指标使用“标签在上、数值在下”的布局，以免圆环挤压长数字。顶部提供“批量查询余额”；点击卡片切换当前站点。
2. 余额查询凭据：
   - `余额查询访问令牌（PAT）`：密码形式，带显示/隐藏按钮。
   - `用户 ID（可选）`：仅在站点必须指定账户时填写；留空时不发送 `New-Api-User`。
   - “保存余额查询凭据”是次级操作；“查询余额”仍是主操作。
   - 令牌与模型测试 API Key 语义上独立。即使值相同，也不共享或复制保存。
3. 结果卡：首次使用空状态说明需要什么；成功后按照“可用 / 已用 / 总额”三列布局，下面显示套餐、查询时间、延迟、模板来源。
4. 模板卡：模板选择下拉中标记“内置 / 自定义”；内置模板只允许“复制为自定义模板”。

### 4.3 模板编辑器

模板编辑器不是对用户脚本的执行器，而是安全的声明式 HTTP 配置表单。

- 顶部必须有返回按钮和“受限 HTTP 配置，不执行任意代码”的说明。
- 分组顺序：基本信息 → 请求配置 → 响应映射 → 可用占位符 → 保存动作。
- 默认只展示必要字段；`已用 / 总额 / 套餐名称 / 成功标记`归入“高级响应映射”。
- 支持占位符：`{{baseUrl}}`、`{{apiKey}}`、`{{accessToken}}`、`{{userId}}`、`{{nowEpochMs}}`。
- 禁止显示展开后的请求头、访问令牌或完整 URL 到日志/Snackbar。
- 系统返回、顶部返回和“取消”必须执行同一个操作：关闭编辑器并回到余额页。

## 5. 动效与流畅性标准

- 一级 Tab 切换：立即切换；不使用页面级大幅平移或重量级过渡。
- 折叠区：避免包裹大卡片的 `animateContentSize`；任何微动效不得造成布局位移。
- 查询中：按钮文字改为“正在查询…”，显示小型进度环，禁止重复点击。
- 成功/失败：使用 Snackbar 显示简短结果；字段错误紧贴字段显示，不能只给顶部笼统错误。
- LazyColumn 的项目必须设置稳定 `key`，长模型列表不一次性创建全部可见项。
- 网络、Keystore、DataStore 必须在非主线程执行；UI 状态只在主线程更新。
- 冷启动不得同步构建未使用的网络客户端；模型/余额 API 与批量测试器按首次真实操作在后台创建。ViewModel Factory 必须在组合中记忆化，避免重组时重复构建依赖。

## 6. 可访问性与安全检查表

- [ ] 每个图标按钮都有内容描述；纯装饰图标显式标为 `null`。
- [ ] 文本字段始终有可见 Label，不只依赖 placeholder。
- [ ] 密钥默认隐藏，显示/隐藏按钮可被 TalkBack 正确朗读。
- [ ] 所有触控目标不小于 48dp，列表操作之间至少 8dp。
- [ ] 错误旁有具体修正提示，例如“此模板需要余额查询访问令牌（PAT）”。
- [ ] 不在 DataStore、导出 JSON、结果卡或日志写入 API Key / PAT 明文。
- [ ] 关闭模板编辑器不会退出 Activity。

## 7. 实施优先级

| 优先级 | 项目 | 验收标准 |
| --- | --- | --- |
| P0 | 安全与稳定 | 密钥隔离、返回正确、无启动崩溃、模板不执行代码 |
| P0 | new-api 余额流程 | PAT、可选用户 ID、请求头省略规则正确，展示 group/可用/已用/总额 |
| P1 | 可用性 | Prompt 预设、折叠高级项、字段级错误、加载反馈 |
| P1 | 视觉一致性 | 主题令牌、卡片层级、统一 4dp 间距与字体层级 |
| P2 | 性能感知 | 列表稳定 key、首屏渐进组合、Factory/API 延迟初始化、重组计数检查 |

## 8. 验收设备与测试

每次 UI/交互改动至少在 Android API 35 模拟器验证：冷启动、Tab 切换、双栏卡片、密钥显示/隐藏、保存凭据、无凭据查询错误、模板编辑返回、Prompt 预设点击、深色与浅色主题、连续上下滚动。提交 APK 前须确认 `AndroidRuntime` 无崩溃记录。

## 9. 配置导入与导出界面（v0.7.2）

### 9.1 交互层级

1. 首页标题为“配置导入与导出”，只用三段说明准确区分两类导出：加密备份会迁移供应商、模型、测试参数和余额模板；它包含 API Key、PAT 和用户 ID，而测试结果 JSON 不包含这些凭据；密码不保存、遗失后无法恢复。操作以同一行等宽的“导出 / 导入 / 关闭”呈现，导出为主按钮。
2. 导出密码页只显示必要输入：备份密码、再次输入和最少 12 字符说明。密码字段为单行、可粘贴、默认遮罩，并提供带明确描述的显示/隐藏按钮；两次不一致的错误紧贴第二个字段。
3. 导入先通过系统文件选择器选取文件，再在“验证配置备份”页输入密码。验证成功后才进入摘要页；摘要清晰列出创建时间、供应商数、自定义模板数、API Key 数和余额令牌数。
4. 摘要后的“确认覆盖当前配置”明确使用错误语义色文字说明其后果，并要求再次输入密码。重选文件和取消保持次级视觉层级，不能误触发覆盖。

### 9.2 视觉与无障碍规则

- 使用 Material 3 `AlertDialog`、语义色和统一间距，不使用新的装饰性图标或横向主流程滚动。
- 每个按钮和图标入口的热区不低于 48dp；加密、验证、恢复期间显示不可关闭的进度反馈并禁用重复动作。
- 文案始终说明“不会自动访问站点”；错误出现在密码字段附近或首页提示中，避免仅用颜色或 Toast 表达失败。
- 弹窗在深色、浅色和增大字号下应保持关键操作可见；若内容空间不足应整体垂直滚动/换行，不能截断“确认覆盖”等关键动作。
- 密码、API Key、PAT 和用户 ID 不显示在摘要、Snackbar、Toast、日志或截图中；图标仅为辅助，文字承担完整含义。

### 9.3 首帧与启动交接（v0.7.2）

1. Android 12+ 的系统启动面结束后，先显示原生的“正在准备 Relay Tester / 正在加载本地配置…”加载层；该层不读取配置、凭据或网络。
2. 原生加载层至少绘制一帧后才开始创建 `ComposeView`，并持续覆盖到 Compose 已绘制首帧。这样在 Compose/ART 冷预热时，用户不会看到空白内容区。
3. 任何 `OnDrawListener` 都必须通过 `View.post` 在回调结束后注销；不得在 `onDraw` 内直接 `removeOnDrawListener`，否则 Android 会直接抛出 `IllegalStateException`。
4. 加载文本与进度指示均为非敏感内容；不显示供应商、地址、模型、API Key、PAT 或用户 ID。系统启动面的实际持续时间由设备和 Android Runtime 决定，不能以高负载模拟器数值推断真机性能。

### 9.4 品牌标识（v0.7.2）

1. 启动图标不再使用“绿色卡片 + 三条横线”。它采用深靛色圆角底座、三枚中继节点和中心验证勾：连线表达中继连接，验证勾表达模型测试结果。
2. 前景采用 `VectorDrawable`，Adaptive Icon 具有同轮廓的 monochrome 版本；桌面、Android 12+ 启动面、原生加载页和顶部标题位重用该标识，不能重新引入不一致的菜单或齿轮图形。
3. 日间启动背景 `#F4F7FF`、夜间 `#0A1024`，与深靛底座维持清晰对比；蓝、紫、青绿仅承担品牌层级，状态仍需搭配文字和图标表达。

详细的数据边界、加密格式、原子性和实际验证记录以 [CONFIG_BACKUP_IMPORT_EXPORT_V5_SPEC.md](CONFIG_BACKUP_IMPORT_EXPORT_V5_SPEC.md) 为准。
