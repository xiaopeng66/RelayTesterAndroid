# Relay Tester Android 1.0.0

发布日期：2026-08-30

## 版本信息

- 版本号：`1.0.0`
- 版本代码：`10000`
- 提交：`d045bc9`
- 更新重点：首个公开版本，提供多供应商模型测试、可配置余额查询、安全存储与完整配置迁移能力。

## 更新内容

- **高级测试**：支持模型列表拉取、并发批量测试、限速、重试、筛选、排序、复制和安全结果导出。
- **模型测试**：支持 Chat Completions、Responses 与 Anthropic Messages 三种协议。
- **余额查询**：内置 `new-api` 模板，并支持新建、复制、编辑、删除受限模板；单站点与全部站点余额查询可用。
- **配置备份**：完整配置导入导出，包括供应商、模型、测试参数、模板和凭据，输出用户密码加密的 `.rtbackup` 文件。
- **界面与交互**：提供多供应商管理与原生 Android 操作界面。
- **稳定性和修复**：优化启动期间后台数据与 Keystore 调度；修复极端本地存储失败时旧密钥过早删除、保存失败后状态误报、上游错误脱敏不完整等问题。

## 验证

- **单元测试**：项目当前没有测试源码。
- **构建任务**：`lintDebug`、`assembleDebug`、`assembleOptimized`、`assembleRelease` 均成功。
- **运行验证**：5 次模拟器冷进程启动均进入 `MainActivity`，后 4 次平均 497 ms；API 34 隔离模拟器无 `AndroidRuntime` 崩溃、无 ANR。
- **回归范围**：模型测试和余额查询主页面截图回归通过；验证期间未发起真实模型或余额请求，也没有导入、导出或清除用户数据。

## 下载

- 文件：`app-optimized.apk`
- 大小：`约 1.5 MB`
- SHA-256：`4D29EA3EB2352ED1E93370CEBBE0F5044AE5A4715561BC3D8CFFFF803F4692D`
- 说明：R8 优化、debug 签名，版本名为 `1.0.0-optimized`，包名 `com.relaytester.app.debug`。
