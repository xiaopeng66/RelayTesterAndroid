# Relay Tester Android 1.1.0

发布日期：2026-08-31

## 版本信息

- 版本号：`1.1.0`
- 版本代码：`10100`
- 提交：`ec2627d`
- 更新重点：模型筛选与快捷筛选增强，余额结果本地恢复，余额卡片布局优化。

## 更新内容

- **模型测试**：模型名过滤与测试结果搜索支持 OR 分隔符，例如 `gpt|claude`；开始测试会准确使用当前筛选范围，避免不需要的请求。
- **高级测试**：新增每个供应商独立保存的快捷筛选词，可添加、删除和多选；快捷词与文本筛选统一按 OR 匹配。
- **余额查询**：成功结果默认保留在本机，重启后可恢复最近一次结果；本地快照仅包含展示所需信息，不含 API Key、PAT、用户 ID、URL、请求头或响应原文。
- **配置备份**：快捷筛选词随加密配置备份迁移；当前选中状态保持为页面会话状态，不会带入其他供应商。
- **界面与交互**：双栏余额卡重排为“圆环右上、已选右下、金额主列全宽”，长金额不再被圆环挤压。
- **稳定性和修复**：切换、更新或删除余额模板，以及删除供应商时，同步清理不再有效的本地快照。

## 验证

- **单元测试**：项目当前没有测试源码，`testDebugUnitTest` 为 `NO-SOURCE`。
- **构建任务**：`compileDebugKotlin`、`lintDebug`、`testDebugUnitTest`、`assembleDebug`、`assembleOptimized` 均通过。
- **运行验证**：优化 APK 在隔离的 `RelayTesterApi35` API 35 模拟器上通过 `adb install -r` 覆盖安装，主界面和余额查询页正常渲染。
- **回归范围**：连续 5 次冷启动耗时 359–470 ms，崩溃缓冲为空，未发现 ANR；验证未调用真实模型或余额 API，也未导入、导出或清除数据。

## 下载

- 文件：`RelayTester-1.1.0.apk`
- 大小：`1.53 MB`
- SHA-256：`80B2A7188822EC9F1878FDB9A17A98F9484874B4427C8ADBEE79847D133D1DA0`
- 说明：R8 优化并启用资源收缩；使用 debug 签名和体验包 `com.relaytester.app.debug`。
