# Relay Tester Android 1.2.0

发布日期：2026-09-08

## 版本概览

1.2.0 聚焦多供应商模型管理、并行测试和移动端布局稳定性。模型测试与余额查询继续保持本地直连模式，不新增中间服务器或账号体系。

## 核心功能

- 模型测试页新增批量拉取模型，可并行刷新所有已配置供应商，并显示完成进度；
- 高级测试支持跨供应商关键词检索，显示匹配模型及来源，并可勾选批量添加；
- 编辑已配置模型时，已添加来源在检索结果中保持选中并锁定，移除来源后自动释放为可选状态；
- 已配置模型支持单个供应商来源测试，多个来源可同时测试；同一来源测试中保留旧结果并显示进行中；
- 模型测试结果卡片只显示可用、HTTP 状态码或失败类型，进度条固定在供应商来源摘要行，不改变列表位置；
- 余额查询和供应商管理继续使用双栏卡片布局，长余额、分组和操作按钮保持稳定避让；
- 站点保存提示提升到配置弹窗上方，不遮挡关闭和保存按钮，并修复提示顶部被窗口边界裁切的问题；
- 配置导入导出支持加密或不加密两种模式。

## 可靠性修复

- 修复多来源并行测试开始时状态未及时同步的问题；
- 修复单个测试异常后来源可能永久显示进行中的问题；
- 修复批量拉取模型时并发更新成功计数的竞态；
- 修复批量拉取失败路径重复推进进度的问题；
- 增加并行来源测试回归测试，验证结果独立保留和并发执行。

## 构建产物

| 产物 | 包名 | 说明 |
| --- | --- | --- |
| `RelayTester-v1.2.0-android.apk` | `com.relaytester.app.debug` | R8 优化、debug 签名的小安装包，用于体验和覆盖同签名调试包。 |

版本信息：`versionName 1.2.0`，`versionCode 10200`。

## 验证摘要

- `compileDebugKotlin`：通过；
- `testDebugUnitTest`：通过，包含多来源并发测试；
- `assembleOptimized`：通过；
- `lintVitalOptimized`：通过；
- `git diff --check`：通过；
- APK SHA-256：`a8e5ef0bee50c5bf959427f1172d6ae574390ec6fcabdffbd230a00e31807c34`。
