# Relay Tester Android：完整配置加密备份与迁移规范（v0.7.0）

**日期：** 2026-08-30  
**状态：** 已完成实现、离线构建与非破坏性 API 35 模拟器验证；完整“导出后覆盖恢复”将留给无真实凭据的测试配置完成。  
**适用范围：** Android 原生应用的本地配置备份与恢复；不向任何站点发送请求，不上传文件。

---

## 1. 需求与结论

用户需要“导出、导入所有配置信息”。应用的“所有配置”包含普通配置和高敏感凭据，不能复用现有的“导出测试结果 JSON”：该功能只导出一次测试结果，并且故意不含 API Key。

本轮新增 **配置备份与迁移**：

- 可从模型测试或余额查询页面的统一入口打开；
- 导出全部供应商、模型列表、测试参数、余额模板、当前站点、余额用户 ID；
- 同时导出模型测试 API Key 与余额访问令牌；
- 凭据只存在于用户设置密码保护的 AES-GCM 加密备份中，绝不输出明文 JSON；
- 可通过 Android 系统文件选择器创建或选择文件，支持跨设备迁移；
- 导入先解密、解析、限制校验并展示摘要；只有用户第二次确认后才替换当前配置；
- 不导出运行时测试结果、余额快照、错误信息或日志，它们不是配置且可能过期。

---

## 2. 数据边界

| 数据 | 导出 | 导入 | 原因 |
|---|:---:|:---:|---|
| 供应商名称、Base URL、协议 | 是 | 是 | 核心站点配置 |
| 已获取的模型列表 | 是 | 是 | 减少迁移后重复获取 |
| 超时、并发、Prompt、重试、批次参数 | 是 | 是 | 测试行为配置 |
| 余额查询模板及已选模板 | 是 | 是 | 自定义站点适配配置 |
| 余额用户 ID | 是 | 是 | 查询上下文 |
| 模型 API Key、余额访问令牌 | 是，加密 | 是，重新写入 Keystore | 用户明确要求所有配置 |
| 当前选中供应商 | 是 | 是 | 保留工作上下文 |
| 测试结果、余额快照、失败消息 | 否 | 否 | 运行时数据，不适合作为迁移配置 |
| Android Keystore 密文、别名、设备密钥 | 否 | 否 | 设备绑定，不能跨设备复用 |

导入后每个密钥和令牌都生成新的本机 Keystore 条目；备份中绝不保存旧的 Keystore `secretId`、密文或 Android KeyStore 密钥。

---

## 3. 文件格式与安全设计

导出文件扩展名为 `.rtbackup`，MIME 类型为 `application/octet-stream`。部分 Android 文件提供方会把自定义扩展名报告为未知 MIME，因此导入选择器允许用户选择任意文件类型；选中后仍由应用的大小、版本、加密信封和字段校验拒绝任何不合法输入。文件为 JSON 外层信封，信封只含版本、KDF 参数、随机盐、随机 IV 与密文，不含供应商名、地址、模板或凭据。

```text
{
  "format": "relay-tester-backup",
  "version": 1,
  "kdf": {
    "algorithm": "PBKDF2WithHmacSHA256",
    "iterations": 210000,
    "salt": "Base64"
  },
  "cipher": {
    "algorithm": "AES/GCM/NoPadding",
    "iv": "Base64",
    "payload": "Base64"
  }
}
```

内部明文只在内存中短暂存在，采用 UTF-8 配置 JSON，包含 `schemaVersion`、`createdAt`、供应商、模板和当前选中站点。安全约束如下：

1. 导出密码至少 12 个字符，确认输入必须一致；密码不保存、不日志记录、不写入文件。
2. 每次导出使用新的 16 字节盐、12 字节 GCM IV 和随机 256 位 AES 派生密钥。
3. AES-GCM 的认证标签会同时检测错误密码和文件篡改；对用户统一提示“密码不正确或备份文件已损坏”，不泄漏细节。
4. 只读取不超过 5 MiB 的文件；拒绝超大、缺少格式标记、版本不支持、Base64 无效或字段不完整的输入。
5. 禁止导入脚本、可执行代码、Keystore 条目、测试/余额运行时结果。余额模板仍是既有的声明式模板，而非 JavaScript。
6. 不新增网络权限、不新增联网依赖；所有 KDF、加解密、文件 I/O、DataStore 和 Keystore 操作均在 `Dispatchers.IO`。

密码并非用来替代 Android Keystore：备份文件使用用户密码保护以支持跨设备移动，恢复后才由新设备的 Android Keystore 保护静态凭据。

---

## 4. 交互与界面规格

统一入口为顶部栏右侧、视觉 24dp/触控 48dp 的“配置备份”图标按钮；模型测试和余额查询页面均可打开同一对话框。

### 4.1 导出

1. 用户点击“导出全部配置”。
2. 显示两次密码输入与安全提醒：备份含 API Key 和余额访问令牌；遗失密码无法恢复。
3. 合法密码后，在后台先保存当前未保存的供应商和余额凭据，再读取所有凭据、生成加密备份。
4. 打开 Android 系统“创建文件”界面，默认文件名为 `relay_tester_backup_时间.rtbackup`。
5. 成功仅提示“配置备份已导出”；取消或写入失败丢弃内存中的加密负载，不改变现有配置。

### 4.2 导入

1. 用户点击“导入配置备份”，通过系统文件选择器选择 `.rtbackup`。
2. 输入该备份的密码；后台解密和结构校验完成前不修改当前配置。
3. 显示摘要：导出时间、供应商数、自定义模板数、包含的 API Key 数和余额令牌数。
4. 用户点击“确认覆盖当前配置”才开始替换；取消、密码错误或格式错误均保留当前配置。
5. 成功后刷新两个页面的供应商/模板/当前凭据草稿，清空旧测试结果与余额快照，提示“配置已导入”。不自动发起模型或余额请求。

对话框不使用布局位移动画；按钮有正常 Material 按压状态，所有操作热区至少 48dp。提示同时有文字，不只依赖安全图标或颜色。

---

## 5. 原子性与失败恢复

导入使用“先准备、后提交”顺序：

1. 解密、解析和限制校验输入；
2. 在本机 Keystore 写入每个新 API Key / 令牌，生成新的不透明 ID；
3. 仅在所有新凭据写入成功后，一次性保存新的 DataStore 状态；
4. DataStore 成功后才删除旧的 Keystore 条目，并一次性发布新的 UI 状态。

若步骤 2 或 3 失败，删除本轮刚创建的 Keystore 条目，原 DataStore 与原供应商仍保持有效。若步骤 4 删除旧条目失败，旧密文可能暂留在私有存储，但不会再被任何供应商引用；这不会破坏导入后的配置。

导入与导出在初始化、模型批测、单站余额查询或批量余额查询期间禁用，避免将正在变化的数据纳入备份或覆盖正在使用的凭据。

---

## 6. 实现范围

| 文件 | 工作 |
|---|---|
| `core/backup/ConfigurationBackupCodec.kt` | 备份数据模型、JSON 编解码、PBKDF2 + AES-GCM、大小与结构限制。 |
| `TesterViewModel.kt` | 生成加密备份、导入预览、确认恢复、失败回滚、两个页面的状态同步。 |
| `MainActivity.kt` | 持有统一的备份对话框打开状态，并从两页传入同一入口。 |
| `RelayAppHeader.kt` | 增加可访问名称明确的 48dp 配置备份按钮。 |
| `TesterScreen.kt`、`BalanceScreen.kt` | 传递共享入口，不改变各自的业务请求。 |
| `feature/backup/ConfigurationBackupDialog.kt` | 密码、系统文件选择器、导入摘要、覆盖确认及安全提示。 |
| `README.md`、开发/重绘文档 | 更新版本、功能、安全说明和测试记录。 |

---

## 7. 验收标准

- [ ] 从两个一级页面均可打开“配置备份”。
- [ ] 导出文件不包含任何可读的供应商、URL、模型、模板、API Key、令牌或用户 ID。
- [ ] 密码少于 12 字符或两次输入不同不能创建备份。
- [ ] 正确密码可显示准确摘要；错误密码、损坏文件、超大文件不修改现有配置。
- [ ] 覆盖确认前不写 DataStore、不写/删 Keystore、不发网络请求。
- [ ] 确认导入后供应商、模型、参数、模板、API Key、令牌、用户 ID、当前站点全部恢复；旧运行时结果/余额快照被清空。
- [ ] 导入失败能够保留原有配置；不在日志、Toast、Snackbar、截图或文档中暴露密钥/令牌/用户 ID。
- [ ] API 35 模拟器覆盖安装后可启动、无 `AndroidRuntime` 崩溃；`lintDebug` 为 0 errors，离线 `assembleDebug` 通过。
- [ ] 在深色模式、1.3× 字号、360dp 宽度和横屏下验证对话框不截断关键操作；密码字段可显示/隐藏并支持粘贴。

---

## 8. 不在本轮范围内

- 云同步、账号登录、多端自动同步；
- 选择性合并、逐站点导入、回滚历史版本；
- 恢复测试结果或余额历史；
- 绕过用户密码恢复备份。

这些能力将来可建立在同一版本化加密格式上，但不得降低本轮“默认加密、先预览后覆盖”的安全边界。

---

## 9. 实施记录

### 9.1 已落地内容

1. 新增 `core/backup/ConfigurationBackupCodec.kt`：使用 `PBKDF2WithHmacSHA256`（210,000 次迭代）派生 256 位 AES 密钥，以 AES-256-GCM 加密版本化的配置 JSON。每次导出重新生成 16 字节盐和 12 字节 IV；外层 JSON 不含站点名、地址、模板、API Key、PAT 或用户 ID 明文。
2. `TesterViewModel` 已实现快照创建、导出密文短暂缓存、导入文件大小限制、密码预览、二次确认和恢复事务。导入时新凭据会先写入当前设备 Keystore，DataStore 提交成功后才清理旧条目；提交前任一步失败会清理本轮新建条目并保持旧配置不变。
3. 新增共享的 `ConfigurationBackupDialog`。模型测试和余额查询的右上角都提供带“配置备份与导入”可访问名称的 48dp 图标入口。导出要求两次输入至少 12 个字符的密码；导入要求“选择文件 → 输入密码验证 → 查看摘要 → 再次输入密码确认覆盖”。
4. 备份包含供应商、Base URL、协议、模型、测试参数、余额模板、当前站点、API Key、余额访问令牌和可选用户 ID；测试结果、余额快照、错误消息和日志不会导出。导入完成不会自动访问模型或余额 API。
5. 编解码器增加了供应商、模板、模型、请求头、字段长度、协议、HTTP 方法和换算除数等限制。内置 `new-api` 模板始终由当前应用重建，不信任导入文件中同 ID 的模板内容。

### 9.2 构建与 API 35 验证

| 项目 | 实际结果 |
|---|---|
| 离线构建 | `:app:assembleDebug` 通过 |
| 静态检查 | `:app:lintDebug` 通过，**0 error**、5 个既有非阻断 warning |
| 安装方式 | `adb install -r` 覆盖安装；未卸载、未清空应用数据 |
| 模拟器 | Android API 35，包名 `com.relaytester.app.debug` |
| 启动检查 | `MainActivity` 恢复到前台，应用进程存在，清空后 `AndroidRuntime` 日志无错误 |
| 备份界面 | 已检查顶部入口、备份说明、12 字符密码门槛、两次密码一致性、显示/隐藏密码、系统文件选择器打开和取消返回路径 |
| 编解码器冒烟测试 | API 35 应用进程内以虚构配置验证：加密、外层无明文、正确密码恢复、错误密码拒绝、篡改密文拒绝和密码字符数组清零均通过；只记录通过标志 |
| 可访问性/适配 | 图标入口具备明确内容描述；按钮触控高度不低于 48dp；此前已复核深色和 1.3× 字号下的关键弹窗布局 |

最终 Debug APK：`0.7.0-debug`（versionCode 10），大小 `17,988,786` bytes，SHA-256 为 `419912BD20DEA755C1C59B75F9994C542AC83291D0EA3D227A4B5CEEFB0594A3`。

### 9.3 验证边界与后续回归

- 当前模拟器保存着用户既有的站点凭据。为避免创建一个用户未知密码、却含有真实凭据的备份文件，也避免覆盖其 Keystore 引用，本次没有执行真实配置的“导出 → 确认覆盖导入”破坏性回归。
- 加密文件格式的正向与异常路径已用完全虚构数据在 API 35 应用进程内验证通过；仍未在当前含真实凭据的模拟器上执行会提交 DataStore/Keystore 的最终覆盖恢复。
- 未调用任何真实模型列表、模型测试或余额接口，也未把 API Key、PAT、用户 ID、站点名或响应内容输出到日志、截图、文档或临时文件。
- 推荐在新建的无敏感凭据测试供应商或独立真机配置上补做完整迁移回归：验证外层文件无明文、正确密码预览/覆盖恢复、错误密码、篡改文件、超大文件以及导入中断回滚。
