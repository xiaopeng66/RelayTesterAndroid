# Relay Tester Android：紧凑交互、统一启动与品牌重绘规范（v0.7.5）

**日期：** 2026-08-30  
**状态：** 已实施；已通过离线构建/lint，并完成重启后的 API 35 模拟器页面复核。  
**范围：** 供应商选择卡、站点操作、协议/Prompt 选择器、余额查询导航、安全边界、品牌标识与启动交接；不改变模型、余额、模板、配置备份或凭据协议。

---

## 1. 本轮问题与结论

1. 供应商卡只承载名称与“协议 · 模型数”两行，却在底部留下明显空白；文字在视觉上偏上。
2. 删除站点作为文字按钮放在“保存站点”旁，离供应商管理入口过远，占据了保存区的注意力。
3. 接口协议和 Prompt 的 48dp 外层点击区域使用默认 ripple，点击时会出现比 36dp 实际胶囊更大的浅色遮罩；不同标签的实际边界和间距也不够整齐。
4. 底部导航的“余额”标签与页面功能名“余额查询”不一致；安全边界使用文字 `•`，尺寸和对齐不足以形成清晰提示层级。
5. 当前标识由深色方形底座包裹节点图形，桌面、系统启动面、原生加载页和顶部品牌位的尺寸/背景不连续，造成“先大白底、后小深底”的两段式观感。

本轮遵循 Android 48dp 最小触控目标、4/8dp 间距节奏、深浅主题语义色和无障碍名称规则；同时避免所有主流程横向滚动或全屏高度动画。

---

## 2. 供应商卡与站点操作

### 2.1 两行卡的高度计算

供应商卡只保留以下内容：

- 名称：20dp 行高以内；
- 元信息：16dp 行高以内；
- 两行之间：0dp 额外间距；
- 上下内边距：各 4dp。

正常字号内容预算为 `20 + 16 + 4 + 4 = 44dp`。卡片改用 `heightIn(min = 52.dp)`，使上下各保留约 4dp 的相同呼吸空间；不使用固定 52dp，以便大字号下自然增高而不裁剪。卡片内层必须 `fillMaxSize()` 与 `Arrangement.Center`，两段文本不再额外添加仅作用于下行的 top padding。

双栏网格的横向与纵向间距均为 **8dp**，卡片宽度按 `(可用宽度 - 8dp) / 2` 计算，避免 10dp 之类的任意间距破坏应用整体的 4/8dp 节奏。

### 2.2 供应商标题行

“供应商”右侧改为一个 48dp 高的操作组：

```text
供应商                                      [ + ] [ 垃圾桶 ]
```

- 加号和垃圾桶都是 24dp Outline 图形、48dp 触控区域；视觉间距为 4dp；
- 垃圾桶使用 `DeleteOutline`，错误语义色，内容描述为“删除当前供应商”；
- 不再在“保存站点”左侧显示“删除”文字按钮；保存区只保留主操作“保存站点”；
- 删除沿用当前业务删除规则和禁用状态，不改变供应商、密钥和模型数据的删除语义。

---

## 3. 协议与 Prompt 选择器

### 3.1 统一的等宽布局

“接口协议”和“快速选择”都使用 `Row(fillMaxWidth)`：

- 相邻项间距固定 **8dp**；
- 每项使用 `weight(1f)`，实际 36dp 胶囊边界等宽、间距完全一致；
- 外层仍为 48dp 高的无障碍点击区域，视觉胶囊为 36dp 高并 `fillMaxWidth()`；
- 长协议名在极窄或大字号环境使用单行省略，不突破本行，也不出现横向滚动。

### 3.2 点击状态

外层点击区设置独立 `MutableInteractionSource` 且 `indication = null`，移除会溢出到胶囊外的浅色 ripple 覆盖层。选中反馈保留在实际胶囊内：描边、容器色和文字色立即切换；这不是仅依赖颜色的状态，因为控件同时保留 RadioButton 语义与 selected 状态。

---

## 4. 余额查询文案与安全边界

1. `AppDestination.BALANCE` 的导航标题统一为“余额查询”；顶部副标题和导航标签保持一致。
2. 安全边界将文字点替换为固定 **8dp** 的圆形强调标记，采用 `tertiary` 语义色；文字与标记相距 8dp，顶部对齐。每条规则仍独立成行，不能仅用颜色传递含义。

---

## 5. 品牌标识与统一启动动画

### 5.1 图形方向

采用代码绘制的原生 VectorDrawable，而不是位图或图标库拼接：

- 不使用深色方形/浅色卡片的二次包裹；Adaptive Icon 的系统背景承担底色；
- 前景为三个等粗节点、三段连接轨迹和一个居中的“已验证”勾，形成可识别的中继拓扑；
- 几何基准统一为 108 × 108 viewport、圆角端点、4dp 等效描边与 8dp 节点半径；
- 桌面图标、主题单色图标、系统启动面、原生加载页和顶部品牌位共享同一前景矢量；顶部不再包裹浅色 `Surface`。

这是面向 Android 全尺寸渲染的专业矢量绘制方案：在 24dp 顶栏、48dp 桌面图标、系统启动缩放和单色主题图标下都保持边缘清晰，优于生成式位图在小图标下的模糊与不可控细节。

### 5.2 启动交接

1. Android 12+ 系统启动面和应用内原生加载层使用同一个 `startup_window_background` 色值；日间与夜间分别由资源限定，禁止 Activity 再从系统默认 `colorBackground` 推断颜色。
2. 系统启动面使用同一前景的 `AnimatedVectorDrawable`，加载层也启动该 drawable；动画是一次 680ms 的中心验证环呼吸，使用 `fast_out_slow_in`，不无限循环、不缩放界面布局。
3. 原生加载标识为 144dp，与系统启动面的视觉重心一致；加载标题/说明/进度条位于相同背景上。Compose 第一帧绘制后才撤下加载层，形成一个连续启动表面而不是白底大图标到深底小图标的跳变。
4. 启动页不读取或展示任何站点、模型、API Key、PAT、用户 ID；动画仅作用于非敏感矢量图层。
5. **绘制回调兜底：** 部分 API 35 模拟器会在 ART 校验期间无限延后被遮挡/近透明 ComposeView 的首帧回调。因此加载层以首帧回调优先撤下，并额外设置 1.2 秒上限；上限到达时直接显示 Compose 内容，避免加载页成为永久阻塞点。

---

## 6. 代码范围

| 文件 | 调整 |
|---|---|
| `TesterScreen.kt` | 52dp 自适应供应商卡、无额外行间距、标题行加号/删除图标组、保存区只保留保存、等宽无 ripple 的选择胶囊。 |
| `AppNavigation.kt` | 导航标签由“余额”改为“余额查询”。 |
| `BalanceScreen.kt` | 8dp 圆形安全边界强调标记与 8dp 间距。 |
| `ic_launcher_foreground.xml`、`ic_launcher_monochrome.xml` | 无外框的中继验证矢量标识。 |
| `av_splash_brand_mark.xml`、`res/animator/*` | 系统与原生加载页共用的一次性品牌呼吸动画。 |
| `MainActivity.kt`、`themes.xml` | 同资源背景、动画启动、144dp 加载标识，消除启动两段色彩跳变。 |
| `RelayAppHeader.kt` | 直接显示品牌矢量，不再使用浅色 Surface 包边。 |
| `strings.xml`、`mipmap-anydpi-v26/ic_launcher.xml` | 启动加载文案改用字符串资源；Adaptive Icon 继续保留在 AAPT 可正确解析的 v26 资源目录。该目录限定仍有一条已验证、不可阻断安装的 lint 提示，不能以移动资源到无版本目录的方式消除。 |
| 开发文档 | 本规范、实施记录、版本、构建与模拟器验证结果。 |

---

## 7. 验收与实测结果

| 验收项 | 结果 |
|---|---|
| 供应商卡 | 已确认：运行中的 API 35 APK 使用 52dp 自适应两行卡，名称与元信息在视觉上居中，未见旧版底部大块空白。 |
| 站点操作 | 已确认：标题右侧依次显示 48dp 加号与 48dp 错误色垃圾桶，视觉间距 4dp；站点配置卡底部仅保留“保存站点”。 |
| 协议与 Prompt | 已实施：三项协议和五项 Prompt 都由等权重 36dp 胶囊、8dp 间距组成；外层 clickable 的 ripple 已关闭，不会绘制超出胶囊的浅色状态层。 |
| 余额查询 | 已确认：Tab 标签与顶部副标题均为“余额查询”；模板安全区显示 8dp 青绿色圆点和三条完整规则。 |
| 品牌与启动 | 已实施并查看：无外框中继验证图形在桌面、原生加载页和顶部品牌位一致；加载页使用统一浅蓝灰/深蓝黑背景和一次性 680ms 动画。 |
| 构建与安装 | 已通过：离线 `:app:lintDebug` 和 `:app:assembleDebug` 成功；API 35 模拟器重启后执行 `adb install -r` 覆盖安装、应用启动和两个 Tab 切换成功。 |
| 稳定与数据保护 | 已通过：筛选后的 `AndroidRuntime` 为 0 条错误；未调用模型、余额或其他站点接口，未清除、导入、导出或覆盖用户配置。 |

最终 APK 为 `0.7.5-debug`（versionCode `15`），大小 `17,789,085` bytes，SHA-256：`24714821EC30AC5EA5A69644C6732CC77FD39F148EAF914754452D81E935A890`。

## 8. v0.7.4 收尾复核

1. 供应商双栏网格统一为 8dp 横向/纵向间距，消除旧版 10dp 间隔对全局 4/8dp 节奏的破坏；卡片仍采用 52dp 自适应最小高度。
2. 启动加载标题与说明改用 Android 字符串资源；顶部品牌组件的 `Modifier` 参数按 Compose 规范排序。这两项已通过重新 lint 复核。
3. `mipmap-anydpi-v26` 中的 Adaptive Icon 必须保留：在本工程 AAPT 链路中把它移至无版本目录会造成 `mipmap/ic_launcher` 资源链接失败。因此保留这一条已验证的目录限定 lint 提示，而不是以不可安装的方式“消除”提示。
4. API 35 模拟器采用覆盖安装、冷启动、余额查询 Tab 与安全边界滚动检查；未发起任何站点请求，也未展示或改动敏感配置。

---

## 9. 截图回归修复：严格文字居中与单一启动面（v0.7.5）

本节覆盖第 2 节与第 5 节中有关供应商卡内部排版、原生加载页和 1.2 秒加载层兜底的旧规则；这些旧规则只保留为已废弃的实现历史。

### 9.1 截图诊断

用户提供的截图确认了两个问题：

1. 卡片外框已是 52dp 左右，但 `Column.fillMaxSize()` 在高度只有最小值、最大高度不受限的测量条件下不会取得预期高度；两行文本按内容高度放在卡片顶部，视觉中心偏上。
2. 第一张大 Logo 画面是 Android 12+ 系统 Splash，随后“正在准备 Relay Tester”的画面则是 `MainActivity` 额外建立的原生 `FrameLayout`。两者顺序叠加，造成用户感知到两张启动页面。

### 9.2 实施规则

1. 供应商卡改由 `Box(fillMaxWidth().heightIn(min = 52.dp), contentAlignment = Center)` 承载只含两行的 `Column`；不在内部叠加偏向某一行的 padding。文本组小于 52dp 时由 `Box` 在上下留下对称空间；系统大字号使内容超高时卡片自然向下增长，不裁剪文字。
2. `MainActivity` 直接调用 `ComponentActivity.setContent`。Android 12+ 保留配置好的平台 Splash 直至 Compose 首帧可绘制，再由系统一次性交接到主页面；彻底移除运行时原生加载页、加载标题、进度环、近透明 ComposeView 和 1.2 秒强制撤层逻辑。
3. 启动页只保留系统 Splash 中的同一枚品牌矢量和一次性 680ms 验证环动效；它不读取、显示或请求任何供应商、模型、API Key、PAT、用户 ID 或站点接口。

### 9.3 验收记录

API 35 模拟器在 `adb install -r` 覆盖安装后冷启动：供应商卡测得约 `52.2dp`；应用层“正在准备 Relay Tester / 正在安全加载本地配置”节点均不存在；`MainActivity` 位于前台，崩溃日志筛选为 0 条。该轮冷启动 `am start -W` 记录 `TotalTime` 为 1,800ms，仅用于当前模拟器诊断，不作为真机性能承诺。

最终 v0.7.5 APK 大小为 `17,789,085` bytes，SHA-256 为 `24714821EC30AC5EA5A69644C6732CC77FD39F148EAF914754452D81E935A890`。离线 lint 为 0 errors、4 条既有非阻断 warning（Gradle / Coroutines / OkHttp 可升级提示，以及 Adaptive Icon 目录限定提示）。
