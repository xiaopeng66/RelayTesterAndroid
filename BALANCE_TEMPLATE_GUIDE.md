# 余额查询模板配置指南

本应用将“余额查询脚本”实现为安全、可修改的 **HTTP 查询模板**。模板不是 JavaScript，也不会运行 Shell、Kotlin 或远程代码；它仅描述一次 HTTP 请求以及如何从 JSON 响应中读取数字。这样既能适配多种中转站，也不会让保存的模板取得设备执行权限。

## 快速开始：new-api

应用首次启动后会预置“new-api 余额查询”模板。这个配置根据 [QuantumNous/new-api](https://github.com/QuantumNous/new-api) 当前源码中的用户路由和响应字段创建：

| 配置项 | 内置值 |
| --- | --- |
| 请求方法 | `GET` |
| 请求地址 | `/api/user/self` |
| 请求头 | `Content-Type: application/json`、`Authorization: Bearer {{accessToken}}`、`User-Agent: RelayTester/1.0`；填写用户 ID 时额外发送 `New-Api-User: {{userId}}` |
| 成功标记 | `success` 等于 `true` |
| 可用余额路径 | `data.quota` |
| 已用额度路径 | `data.used_quota` |
| 总额度 | 自动计算 `data.quota + data.used_quota` |
| 套餐名称路径 | `data.group`（为空时显示“默认套餐”） |
| 显示单位 | `USD` |
| 换算除数 | `500000` |

`new-api` 的这个接口位于用户认证路由下，使用账户级的 PAT/Access Token（Bearer 认证）。它不等同于某些站点用于模型调用的普通中转 API Key。请在“余额查询”页的“余额查询凭据”卡中保存访问令牌；用户 ID 是**可选**的，仅在站点必须指定账户时填写。访问令牌会写入 Android Keystore，而不是 DataStore。用户 ID 留空时，应用会完全省略 `New-Api-User` 请求头，而不是发送空值。

内置模板不能直接删除或覆盖。需要修改时，在“余额查询”页选中它并点击“复制为自定义模板”，即可创建你的可编辑版本。

## 站点与模板的关系

- 供应商的 Base URL 在“模型测试”页统一维护；余额查询 PAT 和可选用户 ID 在“余额查询”页按站点维护。
- 每一个供应商可以选择不同的余额查询模板。
- 余额首页以双栏卡片显示所有供应商；卡片包含可用、总额和已用比例圆环。点击卡片即可切换当前供应商并显示其凭据和模板；“批量查询余额”按站点顺序执行，单站失败不会中断其余站点。
- 模板中 `/path` 从站点根路径解析；因此，即使模型 Base URL 填的是 `https://example.com/v1`，new-api 的 `/api/user/self` 也会访问 `https://example.com/api/user/self`。
- 模板中也可使用 `{{baseUrl}}`，它表示当前供应商中填写的完整 Base URL。
- 为避免向未知目标泄露 API Key，应用只允许模板请求与当前供应商相同主机、相同端口的 HTTPS 地址。

## 可配置字段

### 请求配置

| 字段 | 说明 |
| --- | --- |
| 请求方法 | 支持 `GET` 和 `POST`。`GET` 不允许有 Body。 |
| 请求地址 | 相对路径（如 `/api/balance`）或带 `{{baseUrl}}` 的地址。 |
| 请求头 | 每行一条，格式为 `名称: 值`。认证头需使用 `{{apiKey}}` 或 `{{accessToken}}`。 |
| 请求 Body | 仅 `POST` 使用；通常为 JSON，允许使用占位符。 |

### 响应映射

| 字段 | 说明 |
| --- | --- |
| 可用余额 JSON 路径 | 必填，读取实际可用余额数值。 |
| 已用 / 总额路径 | 可选；填写后会在余额卡片中显示。若接口只返回剩余与已用，可勾选“总额度 = 可用额度 + 已用额度”。 |
| 币种路径 | 可选；接口有返回币种字符串时填写。 |
| 套餐名称路径 | 可选；读取后展示在余额结果卡，例如 `data.group`。 |
| 显示单位 | 接口没有币种字段时使用，例如 `USD`、`CNY`、积分。 |
| 换算除数 | `显示值 = API 原始值 ÷ 换算除数`。填写 `1` 表示不换算。 |
| 成功标记路径和值 | 可选。HTTP 2xx 之外还需要判断业务成功字段时使用。 |

JSON 路径支持普通对象属性和数组下标：

```text
data.balance
result.wallet.available
data.items[0].remaining
```

不支持完整 JSONPath 语法、脚本函数、过滤表达式或执行代码。若某种站点的响应无法用这些路径表示，建议先由站点提供一个简单、安全的余额接口，再在模板中配置该接口。

## 占位符与安全规则

可在地址、请求头或 POST Body 中使用：

| 占位符 | 运行时含义 |
| --- | --- |
| `{{baseUrl}}` | 当前供应商 Base URL |
| `{{apiKey}}` | 从 Android Keystore 临时读取的模型测试 API Key |
| `{{accessToken}}` | 从 Android Keystore 临时读取的余额查询访问令牌 / PAT |
| `{{userId}}` | 当前站点配置的可选余额查询用户 ID；作为空请求头值时会省略该请求头 |
| `{{nowEpochMs}}` | 当前 Unix 毫秒时间戳 |

请不要把真实 API Key、Cookie、密码或 Token 直接写进模板。尤其认证请求头应写为：

```text
Authorization: Bearer {{accessToken}}
```

应用会拒绝保存 `Authorization`、`X-API-Key`、`Cookie` 等认证头中的明文密钥。余额接口的响应内容也不会作为错误详情显示，以免敏感字段出现在页面或日志中；单次响应大小限制为 512 KiB。

## 新建一个自定义模板的建议流程

1. 在“模型测试”页保存该站点的 HTTPS Base URL；若模板使用 `{{apiKey}}`，也保存模型测试 API Key。
2. 模板使用 `{{accessToken}}` 时，在“余额查询”页先保存 PAT；只有目标站点要求时才填写 `{{userId}}`。
3. 点击“新建”，先填写名称、请求方法、地址和请求头。
4. 填写最关键的“可用余额 JSON 路径”和换算除数。
5. 只有接口确实返回已用/总额/币种/套餐名或业务成功标记时，再展开“高级响应映射”填写它们。
6. 点击“保存并查询当前站点”。如果失败，错误会提示 HTTP 状态、网络/超时问题，或哪个映射路径没有读到数值；不要把实际密钥粘贴到错误描述或模板说明中。

## 常见排查

| 现象 | 检查项 |
| --- | --- |
| HTTP 401 / 403 | 确认使用的是站点允许查询余额的账户级凭据，而不是只供模型转发的 token。 |
| “成功标记不一致” | 比对接口 JSON，修正成功标记路径和值，或将两者留空，仅按 HTTP 2xx 判断。 |
| “未能读取数值余额” | 检查可用余额路径是否存在；该字段必须是数字或可转换为数字的字符串。 |
| 查询到的数值太大/太小 | 调整换算除数。默认 new-api 使用的 `500000` 沿用你提供的初始面板换算规则。 |
| 地址被拒绝 | 模板不可跳转到不同主机，修改当前供应商 Base URL，或让目标站点提供同主机的余额接口。 |
