# Relay Tester Android：启动、紧凑布局与性能优化实施规范

**版本：** 0.5.0 规划稿  
**日期：** 2026-08-30  
**状态：** 本轮实施的唯一验收依据；先更新本文档，再改代码与生成 APK。

---

## 1. 背景与本轮目标

上一版已解决模型列表横向滑动、测试结果高频发布和余额页多站点能力，但 API 35 模拟器实际复核及用户反馈发现仍有三类问题：

1. **首次打开卡顿**：`TesterViewModel` 初始化后会读取 DataStore、解析完整 JSON、刷新内置模板、读取 Keystore 密钥，并在每次启动时回写完整配置。恢复协程默认从主调度器开始，JSON 解析和序列化可能抢占首帧；恢复过程也会多次发布 UI State，导致首屏重复组合。
2. **模型测试页供应商区过大**：双栏卡片最小高度为 96dp，卡片内实际只有两行文字，造成明显空白；“添加供应商”占据一个完整双栏单元，重要性和视觉面积不匹配。
3. **余额总览密度与文本约束不正确**：
   - “全部站点余额”标题后的 `n 个站点` 未为标题列预留宽度，窄屏上可能被压成逐字竖排；
   - 余额卡固定最小高度 176dp，但信息量不足，形成大量无效留白；
   - 54dp 用量圆环占据双栏卡片过多横向空间，挤压“可用余额 / 总额”，使数值显示不完整。

本轮目标是**先让应用尽快出现且可交互，再完成后台恢复；在不牺牲 Android 48dp 触控目标、可访问性、深色模式与动态字号的前提下压缩信息卡片**。不改变任何模型测试、余额查询、模板或密钥保存的业务语义。

---

## 2. 问题定位与根因

| 现象 | 已确认的代码路径 | 根因 | 本轮处理 |
|---|---|---|---|
| 冷启动时首屏卡、加载后突然整体变化 | `TesterViewModel.restore()` | DataStore 的读取结果在协程上下文中解析；随后 `saveProfiles()` 在每次启动都序列化并回写完整配置；`installDraft()`、`_uiState.update()`、`publishBalanceState()` 连续发布多次状态 | 数据读取、JSON 解析、模板整理和 Keystore 读取放到 `Dispatchers.IO`；将恢复结果聚合为一次模型状态发布；移除无数据变更时的启动回写 |
| 首次启动读取密钥耗时 | `KeystoreSecretStore.getOrCreateKey()` | 同一进程内恢复 API Key 与 PAT 时，旧实现会分别装载 Android Keystore；系统密钥库装载属于 Binder/磁盘相关工作，重复执行会拉长恢复尾部 | 缓存已取得的 `SecretKey`，并以同步初始化防止并发重复装载；密钥仍只由 Android Keystore 保管，密文格式不变 |
| 启动时加载了当前页面尚不需要的网络组件 | `TesterViewModel.factory()` 与 `MainActivity` 组合 | 工厂在 ViewModel 创建时同步构造模型、余额两个 OkHttp 客户端及批量测试器；同时 `viewModel(factory = …)` 的工厂表达式可随重组被重新求值 | 工厂只组装轻量存储依赖；两个 API 客户端和测试器改为首次实际请求时在后台按需创建；Activity 记忆化 ViewModel Factory，避免组合过程重复建立工厂 |
| 供应商卡空白过多 | `SupplierSelector()` | 最小高度 96dp、12dp 内边距、添加项使用整张卡 | 普通供应商卡改为紧凑 72dp 最小高度；“添加供应商”改为标题行右侧 `IconButton`，视觉图标 24dp、点击区域 48dp |
| `n 个站点` 竖排 | `BalanceOverviewHeader()` | 标题 `Column` 与计数 `Text` 在 `SpaceBetween` 行内没有权重和单行约束；说明文本优先抢占宽度 | 标题列使用 `weight(1f)`，计数使用不换行、尾部对齐、最小宽度与省略策略；计数永远作为横向标签显示 |
| 余额数字被圆环挤压、卡片留白 | `BalanceSupplierCard()` / `UsageRing()` | 54dp 圆环 + 58dp 容器在两列卡中占比过高；指标用“左右拉开”的行布局，长数值可用宽度不稳定；176dp 最小高度大于内容需求 | 圆环改为 40dp 容器/36dp 画布/4dp 描边；指标改为纵向的标签 + 单行数值；卡片最小高度目标 132dp，并让状态文本稳定为一行 |

---

## 3. 产品与布局决策

### 3.1 模型测试页：供应商选择

页面结构调整为：

```text
供应商                                      [+]
┌───────────────────┐ ┌───────────────────┐
│ 站点 A              │ │ 站点 B              │
│ Chat · 12 个模型     │ │ Responses · 3 个模型 │
└───────────────────┘ └───────────────────┘
不同站点的模型列表、测试参数和密钥相互隔离。
```

规范：

- 标题与添加按钮处于同一行；按钮靠右，视觉图标为 24dp，加号触控范围为不小于 48 × 48dp，具有“添加供应商”的读屏名称。
- 供应商卡的最小高度为 **72dp**，上下内边距 10dp，卡内只保留供应商名和“协议 · 模型数”。
- 双栏、非横向滑动结构保持不变；卡片短内容不得为了填满高度而强制居中。
- 选中状态必须同时具备容器色、边框/指示和可访问的 selected 语义，不能仅依赖颜色。

### 3.2 余额页：总览标题与供应商余额卡

总览标题行：

```text
全部站点余额                         12 个站点
选择卡片后配置该站点凭据；余额仅来自站点 API。
[ 刷新图标  批量查询余额 ]
```

- 左侧说明列使用剩余宽度，右侧计数永远 `maxLines = 1`、`softWrap = false`、右对齐；任何宽度下禁止逐字竖排。
- 站点数量是标签，不得挤压标题，也不得触发横向滚动。

余额卡结构：

```text
站点名称                                  当前
可用余额                                  12.50
总额                                      20.00     ◔ 38%
已更新 10:42
```

- 卡片目标最小高度 **132dp**；允许随 1.3× 及以上系统字体自然增高，禁止裁剪文本。
- 指标的标签与数值采用纵向文字层级，数值单行省略，不使用 `SpaceBetween` 将数值挤到极小区域。
- 圆环画布为 **36dp**，容器为 **40dp**，描边 4dp；圆心显示已用百分比。圆环只是辅助信息，卡片还保留“可用 / 总额”文字，满足“颜色不能是唯一信息来源”。
- 状态文本一行显示，长错误以尾部省略呈现；完整错误继续在“当前站点查询详情”中显示。

### 3.3 统一间距与可访问性

- 继续采用 4/8dp 节奏：页面边距 16dp，网格间距 12dp，紧凑卡片内边距 10–12dp。
- 小型图标按钮的视觉尺寸可为 24dp，但交互尺寸不得低于 48dp。
- 所有图标使用 Material 矢量图标；视觉相邻文字已表达含义时，图标不重复进入读屏树。
- 不引入基于高度/宽度的装饰动画；按压反馈采用 Material 状态层，避免布局位移造成滚动抖动。

---

## 4. 启动性能实施方案

### 4.1 启动临界路径

1. `MainActivity.onCreate()` 仅设置主题、创建 ViewModel 与轻量 Loading 首屏，不在 Activity 中读取磁盘、网络或 Keystore。
2. `TesterViewModel` 启动后立即切换到 `Dispatchers.IO`：
   - 读取 DataStore；
   - 解析供应商和模板 JSON；
   - 仅刷新内置模板的内存定义；
   - 读取当前活动供应商的 API Key 与 PAT；
   - 构造一个完整的启动快照。
3. 回到主调度器后一次性提交 `TesterUiState` 与 `BalanceUiState`，使页面从 Loading 直接进入稳定状态，而不是先展示空供应商、再插入草稿、再修改初始化标志。
4. 启动期间**不无条件执行 `saveProfiles()`**。默认站点创建、活动站点纠正、内置模板定义刷新均先作为内存迁移；下一次用户明确保存配置、获取模型、修改模板等正常持久化操作时再写入。这样不会覆盖用户配置，也不会在无修改启动时产生额外 DataStore 序列化/磁盘写入。
5. 读取失败时仍保持“安全空白状态、不覆盖旧数据”的既有约束，并在首屏可交互后给出脱敏错误提示。
6. ViewModel Factory 只创建 `SupplierStore` 与 `KeystoreSecretStore`；`RelayApi`、`BalanceApi` 与 `BatchTestRunner` 延迟到用户获取模型、开始测试或查询余额时创建。首次创建必须位于 `Dispatchers.IO`，不抢占首帧或点击反馈帧。
7. `KeystoreSecretStore` 在进程内缓存已由系统 Keystore 返回的 AES 密钥句柄；首个读取仍执行硬件/系统级校验，后续同进程读取不重复加载密钥库。不得缓存 API Key/PAT 明文，不得改变 GCM 加密、密文存储位置或删除策略。
8. `MainActivity` 使用 `remember(applicationContext)` 保存 ViewModel Factory，避免 Compose 因主题或状态重组再次同步创建工厂对象。
9. 模型测试页首次从 Loading 切换到内容时，先组合当前可见的供应商选择与基础站点配置；测试参数、已获取模型/结果列表和安全说明在首个内容帧提交后再加入惰性列表。该延后窗口只用于拆分首次组合负载，不阻塞输入、不发起网络请求，也不改变任何表单默认值或已获取模型数据。

### 4.2 不在本轮盲目采用的方案

- **不在 Compose 中做同步 JSON/Keystore 读取**：会直接阻塞帧调度。
- **不以降低密钥保护来换取启动速度**：API Key/PAT 仍然只保存在 Android Keystore 保护的密文记录中。
- **不在每次启动预取模型或余额**：这会消耗用户额度、延迟首屏并可能向未授权站点发请求。
- **不把所有页面提前组合**：当前只组合当前 Tab；余额页和模板编辑器继续按需进入。

### 4.3 后续可选技术（不作为本轮阻塞条件）

1. **Baseline Profile + ProfileInstaller**：在 release 包中为启动、`MainActivity`、Compose 首屏和常用序列化路径提供 ART 预编译提示，改善已安装后的冷启动。需要在真机收集稳定基线后再接入，不能用猜测 profile 代替测量。
2. **Macrobenchmark / Perfetto**：在多供应商、大模板数据下记录 `timeToInitialDisplay`、`timeToFullDisplay`、主线程帧时间和 I/O trace，用于后续性能回归门禁。
3. **供应商管理独立页**：若站点数量长期超过约 12 个，将测试页的 `FlowRow` 换成独立的惰性列表管理页，避免一次性组合大量卡片；本轮保持用户要求的双栏快速选择。

---

## 5. 实施步骤

| 顺序 | 修改范围 | 完成定义 |
|---|---|---|
| 1 | `TesterViewModel` | 新增 IO 启动快照加载；取消无条件启动回写；初始化状态一次性发布；失败安全策略不回退 |
| 2 | `TesterScreen` | 紧凑供应商卡、标题行右侧 48dp 加号按钮、删除全尺寸添加卡 |
| 3 | `BalanceScreen` | 修复站点计数单行约束；压缩余额卡和圆环；为长数值提供稳定宽度与省略 |
| 4 | 启动依赖 | Factory 记忆化；网络 API/批量测试器按需后台初始化；Keystore 单进程密钥句柄缓存 |
| 5 | Compose 首屏 | 首帧只组合当前可见的基础表单；首帧提交后再加入屏幕外的测试参数与模型列表 |
| 6 | 文档 | 同步主实施文档、UI 重绘文档、README 版本与行为说明 |
| 7 | 验证 | `lintDebug`、`assembleDebug`、覆盖安装、冷启动、日志、浅色/深色/横屏/1.3× 字体截图、上下滑动帧数据 |

---

## 6. 验收标准

### 6.1 功能与布局

- [x] 测试页供应商卡没有明显无效留白，最小高度为 72dp，仍保持双栏和选中态。
- [x] “添加供应商”是供应商标题行右侧的小型加号按钮，不再占用一个网格卡片。
- [x] 余额标题右侧 `n 个站点` 始终横向单行显示，不出现逐字竖排、横向滚动或遮挡。
- [x] 余额双栏卡最小高度为 132dp；可用余额、总额与已用圆环可同时完整辨认。
- [x] 36dp 圆环不会侵占数值区域；可用、总额仍有文字标签。
- [x] 1.3× 字体、深色模式和横屏下文本不裁剪、控件不遮挡、无横向溢出。

### 6.2 性能与稳定性

- [x] 冷启动时 DataStore 读取、JSON 解析、Keystore 解密与启动持久化不占用主线程临界帧。
- [x] 冷启动不构造未使用的 `RelayApi`、`BalanceApi` 或 `BatchTestRunner`；Factory 在重组时不重复同步创建。
- [x] 同一进程恢复 API Key/PAT 时，Android Keystore 只执行一次密钥库装载；不缓存 API Key/PAT 明文。
- [x] 模型测试页首个内容帧不同时预组合屏幕外的测试参数和模型列表；延后内容会在首帧后无阻塞地补齐。
- [x] 无数据变更的启动不会写入 DataStore。
- [x] 首屏初始化最多从 Loading 状态转为完整内容一次；不出现空白供应商/草稿跳变。
- [x] API 35 模拟器冷启动达到或优于本轮优化前基线；已记录 `am start -W` 与启动 `gfxinfo` 样本。
- [x] 连续上下滑动 10 轮的 v0.5.0 有效样本没有连续掉帧、ANR 或 `AndroidRuntime` 崩溃；v0.5.1 未改动已稳定的余额滚动布局。
- [x] `:app:lintDebug` 为 0 errors，`assembleDebug`、覆盖安装、`MainActivity` 前台检查通过。

### 6.3 安全边界

- [x] APK、日志、Snackbar、导出文件和开发文档不输出 API Key/PAT 原文。
- [x] 不调用、修改、清空或删除任何未得到授权的真实站点凭据。
- [x] 迁移失败不清除或覆盖既有 DataStore/Keystore 内容。

---

## 7. 实施记录

### 7.1 实施文件与启动链路

| 文件 | 已实施内容 |
|---|---|
| `MainActivity.kt` | 用 `remember(applicationContext)` 保存 `TesterViewModel` Factory，避免组合重组重复构建启动依赖。 |
| `TesterViewModel.kt` | 保留 IO 启动快照和单次状态发布；`RelayApi`、`BalanceApi`、`BatchTestRunner` 改为惰性按需创建；首次获取模型、模板校验和测试器初始化移至后台。 |
| `SecretStore.kt` | 为 Android Keystore 返回的 AES `SecretKey` 增加线程安全的进程内句柄缓存，不保存 API Key/PAT 明文。 |
| `TesterScreen.kt` | 首个稳定内容帧仅组合供应商和基础配置，测试参数/模型列表/结果在 200ms 后无阻塞补齐，避免 LazyColumn 预取一次创建屏幕外长表单。 |
| `SupplierStore.kt` | 继续将 DataStore 首次读取和 JSON 反序列化留在调用方的 IO 快照中，不在启动完成后回写迁移结果。 |
| `BalanceScreen.kt` / `TesterScreen.kt` | 落实 72dp 供应商卡、标题行加号、余额标题单行计数、132dp 余额卡和 36dp 圆环。 |

### 7.2 当前构建与 API 35 模拟器结果

- **版本与包名：** `0.5.1-debug`（versionCode `8`），`com.relaytester.app.debug`。
- **APK：** `app/build/outputs/apk/debug/app-debug.apk`，`17,871,480` bytes，SHA-256 为 `540552660A6BB9EC91E74BC42DADE9D7EEC4229236B27866C76CFEBDCA7D9C23`。
- **静态检查：** `:app:lintDebug` 和 `:app:assembleDebug` 均通过；Lint 为 0 errors、4 条既有非阻断警告（上游版本提示和 adaptive icon 目录提示）。
- **设备：** `RelayTesterApi35`，Android API 35，1080 × 2400，420 dpi。通过 `adb install -r` 覆盖安装，未卸载或清空用户数据。
- **冷启动：** 连续 5 次 `force-stop → am start -W` 为 1542ms、1492ms、1478ms、1512ms、1522ms；范围 `1.478–1.542s`，中位数 `1.512s`。`MainActivity` 每次都在前台，`AndroidRuntime` 崩溃日志为 0。
- **启动帧边界：** 独立的启动 `gfxinfo` 只采集到 7 帧，包含 Android 进程、ART 与 Compose 的首次类加载，不能用作稳定滚动评分。样本中无慢位图上传；高耗时帧会在启动阶段集中出现，建议最终在真机按同一脚本复测。

### 7.3 已知边界

1. 模拟器保存的是示例站点，未为了性能测试调用真实模型或余额 API，也没有输出 API Key/PAT。
2. 既有余额页有效滑动样本为 950 帧、2.32% 卡顿帧、P90 11ms、P95 17ms、P99 18ms；本版变更启动链路，不改变已稳定的余额滚动布局。
3. 模型页的历史帧统计会受该模拟器中 PocketBridge 前后台抢占影响。当前 APK 无崩溃、无 ANR；仍建议在用户真机安装后根据实际站点数量复测首开体感和连续滚动。
