# Relay Tester Android：界面细节与启动感知优化规范（v0.6.0）

**日期：** 2026-08-30  
**状态：** 本轮先更新本规范，再改 UI、启动主题和 APK；完成后填写第 7 节实施记录。  
**范围：** 只处理用户反馈的显示问题与首开体验；不改变模型测试、余额模板、请求协议、凭据格式或任何真实站点数据。

**后续关联：** 完整配置的加密导入/导出另见 `CONFIG_BACKUP_IMPORT_EXPORT_V5_SPEC.md`；其安全与迁移范围不在本规范内。

---

## 1. 用户反馈与本轮目标

本轮基于 API 35 模拟器截图以及用户反馈，确认需要处理以下问题：

1. 模型测试页的供应商卡虽已缩小，但文本偏上，底部留白显著，视觉重心不在中间。
2. 测试 Prompt 的五个快捷选项在窄屏下让“回复ok”落到第二行；用户要求始终为单行。
3. 余额页顶部副标题过长，应与模型测试页保持同一层级，统一为四个字“余额查询”。
4. “全部站点余额”的说明尾句“余额仅来自站点 API”造成不必要换行；应保留操作指引并缩短为单行。
5. 未配置 Base URL 的余额供应商卡会因内容自然测量而与已配置卡产生高度差；双栏同一行必须对齐。
6. 余额 PAT / 用户 ID 字段的辅助说明继承了 `OutlinedTextField` 的内部缩进，与卡片标题和上方说明不左对齐。
7. “当前站点查询详情”将可用、已用分行，随后总额再独占一行；金额没有足够层级和语义色，扫描效率低。
8. “模板安全与占位符”独立卡片直接罗列技术占位符，作用不清晰、信息密度低；安全说明应在选择模板的上下文中出现，详细占位符保留在模板编辑器。
9. 首开性能虽已完成后台恢复与惰性网络客户端，但 `windowDisablePreview=true` 仍可能让用户在 Activity 首帧前看到无反馈窗口。需要使用原生启动屏保证“正在加载”的状态可见，而不是用复杂 Compose 动画掩盖加载。

本轮目标为：**更紧凑、更可扫描、更稳定；并让启动阶段始终有与品牌一致的系统级反馈。**

---

## 2. 设计与性能原则

- 继续遵循 Material 3 的 48dp 最小触控区域。视觉尺寸可更小，但点击边界不能缩小。
- 不使用横向主流程滚动；Prompt 快捷选项必须在 360dp 宽度、系统字体 1.3× 内保持一行。
- 卡片以固定的视觉高度维持网格对齐；在极端字号导致文本无法安全容纳时，应优先自然增高整个网格行，不能裁剪。
- 金额颜色只表达层级，文字标签始终保留：可用 = `tertiary` 强调，已用 = `primary`，总额 = `onSurface`。
- Android 12+ 使用系统启动窗口属性（`windowSplashScreenBackground`、`windowSplashScreenAnimatedIcon`），静态绘制、无循环动画、无网络；其作用是消除启动窗口的空白感，不把它作为伪造的进度条。离线 SDK 未公开 `Theme.SplashScreen` / `postSplashScreenTheme`，因此由 `MainActivity` 在创建 Compose 前切回正常主题。Android 26–30 使用同色启动窗口作为兼容回退。
- DataStore / Keystore / JSON / 网络客户端的后台延迟加载策略继续保持；本轮不在启动阶段预取模型或余额。

---

## 3. 详细布局规格

### 3.1 模型测试：供应商与 Prompt

供应商卡：

- 固定目标高度 **68dp**（不低于 48dp 触控要求）；内部为上下居中的两行文本，左右内边距 12dp。
- 名称一行尾部省略；次行显示“协议 · 模型数”。卡片内容区域使用 `fillMaxSize + Arrangement.Center`，消除只有底部留白的视觉错位。
- 双栏宽度与标题行右侧加号维持不变。

Prompt 快捷选项：

- 保留原始值：`ping`、`Hi`、`Say OK`、`1+1=?`、`回复ok`。
- 将标准 `FilterChip` 更换为紧凑的选项控件：视觉高度 36dp，外围交互高度 48dp，最小宽度 48dp，选项之间 4dp。
- 文本采用 `labelSmall`；五项在 360dp 内容宽度及 1.3× 字号下保持同一行。
- 已选态使用 `secondaryContainer` + `onSecondaryContainer`，同时保留 RadioButton selected 语义；不只依赖颜色。

### 3.2 余额总览与供应商卡

- 共享顶部副标题改为 **“余额查询”**；底部 Tab 名仍维持“余额”，避免一级导航过宽。
- 总览卡说明改为 **“选择卡片后配置该站点凭据。”**，站点数保留在右侧单行标签。
- 余额供应商卡固定为 **132dp**。`LazyVerticalGrid` 单元内卡片使用完整单元高度，避免“未配置站点地址”与其他卡因内部文本测量而不等高。
- 卡内容使用稳定的三段式垂直分布：站点名/选择状态 → 可用与总额 + 圆环 → 状态。所有状态文本一行尾部省略。
- 可用余额数值使用 `tertiary`，总额保持主文字色；圆环和状态逻辑不变。

### 3.3 余额凭据

- 卡片标题统一为“余额查询凭据”；站点名称作为其下单行上下文，不再将长站点名与标题拼接为一个可能折行的长标题。
- “余额访问令牌可与模型测试 API Key 不同，并会加密保存在本机。”保留为卡片左边界对齐的简短说明。
- PAT 与用户 ID 的说明移出 `OutlinedTextField.supportingText`，改为字段下方的普通 `Text`，与字段左边界和上方说明严格左对齐。
- 文案压缩为：PAT“仅用于余额接口，保存后受 Android Keystore 保护。”；ID“可留空；仅在站点要求指定账户时填写。”错误文案仍在对应字段正下方显示。

### 3.4 当前站点查询详情

成功结果使用一行三列指标：

```text
可用              已用              总额
12.50 USD         7.50 USD          20.00 USD
```

- 三列等宽；数值单行尾部省略，并为读屏保留“标签 + 完整数值”的语义。
- 可用使用 `tertiary`、已用使用 `primary`、总额使用 `onSurface`；不会把颜色作为唯一信息来源。
- 套餐名称与请求元信息压缩为两条单行辅助信息：套餐 / 延迟，以及模板 / 更新时间，避免总额被挤到下一行。
- 尚未查询与错误状态使用现有可恢复说明和查询按钮，不显示伪造金额。

### 3.5 模板安全说明

- 删除独立的“模板安全与占位符”卡片。
- 在“余额查询模板”卡的末尾加入内联“安全边界”：
  - “模板不是脚本；只能访问当前站点的 HTTPS 接口。”
  - “密钥仅在请求时注入；占位符请在编辑模板页查看。”
- 原始占位符名称只在模板编辑器的“可用占位符”上下文内展示，避免普通查询页堆叠技术细节。

---

## 4. 启动链路优化

### 4.1 原生启动屏

1. 在 `values-v31` 的启动主题定义 Android 12+ 系统启动窗口属性；浅色/深色 `windowSplashScreenBackground` 与应用背景同色，使用现有启动图标。当前离线 SDK 缺少可链接的 `Theme.SplashScreen` / `postSplashScreenTheme`，故不引入不能离线构建的 `core-splashscreen`，改由 `MainActivity` 在 `super.onCreate` 前切回正常主题。
2. 在 API 26–30 使用同名启动主题和同色窗口背景作为兼容回退；不新增需要联网解析的依赖。
3. `MainActivity` 的 Activity Theme 使用启动主题；进入 Activity 后，在创建 Compose 内容前立即切换到正常主题，不等待网络或人为延长启动屏。
4. 移除 `android:windowDisablePreview=true`；该标记会让窗口在 Compose 首帧前缺乏可见反馈。

### 4.2 保持的性能边界

- 启动屏不持有 ViewModel，也不等待网络或人为延长展示时间。
- `TesterViewModel` 的 IO 快照、单次状态发布、Keystore 句柄缓存、DataStore 无变更不回写、网络 API 惰性创建维持不变。
- 继续在冷启动、深色模式、1.3× 字体、小屏和横屏下验收；不清空模拟器数据，不调用真实站点。

---

## 5. 计划修改范围

| 顺序 | 文件 | 修改 |
|---:|---|---|
| 1 | 本文档、`DEVELOPMENT_IMPLEMENTATION_SPEC.md`、`UI_REDRAW_SPEC.md`、`README.md` | 先记录本轮的设计、验收与后续实施数据。 |
| 2 | `TesterScreen.kt` | 供应商卡垂直居中/68dp；Prompt 紧凑单行选项。 |
| 3 | `BalanceScreen.kt` | 余额标题、总览说明、固定网格卡、凭据对齐、三列金额、模板安全内联化。 |
| 4 | `MainActivity.kt`、`themes.xml`、颜色资源、`build.gradle.kts`、Manifest | 采用官方原生启动屏、移除空白窗口策略、升级构建版本。 |
| 5 | APK 验证 | 离线 lint/build、覆盖安装、冷启动、AndroidRuntime、浅/深色、小屏、1.3× 字体、横屏和截图复核。 |

---

## 6. 验收清单

- [ ] 供应商卡的两行文字垂直居中，底部不再明显留白；卡片目标高度为 68dp。
- [ ] 五个 Prompt 快捷选项在 360dp 宽度、1.3× 字号下保持单行，且每项触控区域不小于 48dp。
- [ ] 余额页共享标题为“余额查询”，总览说明不出现不必要折行。
- [ ] 未配置地址与已配置站点的余额卡在同一网格行等高、对齐；可用余额有明确强调色。
- [ ] PAT、用户 ID 说明及错误文本左侧与字段和卡片说明对齐。
- [ ] 当前站点详情的可用/已用/总额同一行可扫描；标签、颜色、读屏语义完整。
- [ ] 不再展示独立“模板安全与占位符”卡；模板卡内安全说明简短且指向编辑页。
- [ ] 首开有原生启动反馈，不显示空白预览窗口；不增加启动网络、同步 I/O 或明文凭据缓存。
- [ ] `lintDebug` 0 errors、`assembleDebug`、覆盖安装、冷启动和 `AndroidRuntime` 检查通过。

---

## 7. 实施记录

待代码实现、构建和 API 35 模拟器复测后填写。
